# ASSESSMENT_DEBUG_TRACKER — running tracker for docs/assessment-tool-debug-plan.md + docs/phase-prompts.md

Source directive: `docs/assessment-tool-debug-plan.md` (the phase map, BUG specs, done-when criteria) paired with `docs/phase-prompts.md` (the per-phase prompt text). Severity order per the plan doc: **P0 → P1 → P2 → P4 → P8 → P3 → P5 → rest**.

This file is the single source of truth for progress on that directive. Update status/evidence as work lands; do not rewrite history.

**If the user says "continue" (or similar, with no other context) in a session on this repo:** read this file's Session log (bottom) first, re-verify anything marked "in progress" against live state (don't assume it finished — re-check a background job's real DB/process state), then resume in the severity order above without re-deriving what's already recorded here. Per explicit user instruction (2026-08-30): work autonomously through phases without pausing for confirmation or verification from the user, except before genuinely destructive/irreversible actions against shared state (e.g. running the production data-wipe script for real, force-pushing, deleting live DB tables) — those still get a explicit call-out. When the session's token budget is running low, stop at a clean phase boundary, commit the tracker update, and tell the user to start a new session and say "continue."

Status values: `todo`, `in-progress`, `done`, `blocked`.

Relationship to prior directives: `docs/APP_COMPLETION_PLAN.md` (Phases A-H, all done as of its Session 5) and `docs/NEET_TOOL_FIX_TRACKER.md` / `docs/ASSESSMENT_FIX_TRACKER.md` (both fully done) already did substantial work touching the same subsystems (session/attempt lifecycle, dashboard analytics, demo account, i18n groundwork). This new debug plan is a **later, more comprehensive live-QA pass** — some BUG items here may turn out to already be fixed by that earlier work (verify against current code before assuming a regression exists), others are genuinely new findings.

---

## Phase status

| Phase | Theme | Status | Notes |
|---|---|---|---|
| P0 | Recon & knowledge capture | done | `CONTEXT.md` + `DEFECT-BACKLOG.md` committed. Real root causes found for BUG-03/04/05/06/07/11/15/16/17 and the demo account. |
| P1 | Data reset + demo account lifecycle | done | BUG-01 + BUG-02, see below |
| P2 | Session & test-state integrity | done | BUG-03/04/05/06/07/08, see below |
| P3 | Test console UX & layout | done | BUG-09 (already satisfied), BUG-10 fixed + verified live at all 3 widths, see below |
| P4 | Question content correctness | done | BUG-11/12/13/15, BUG-14 deferred, see below |
| P5 | App-wide EN/TA language system | done (partial — see DB-05) | BUG-16/17 architecture fixed + verified live; full string-coverage sweep explicitly deferred, see below |
| P6 | Syllabus module | paused at user's request (code side done) | BUG-18: real root cause found (all 82 Drive files private, not a code bug); code-side mitigations done; user explicitly said to hold this until they deal with Drive sharing themselves — do not pick back up unless asked |
| P7 | Study plan, tasks, notes, pomodoro | done | BUG-19/20/21/22 — all 4 built for real (new DB tables/routes/frontend), verified live over HTTP and in a real browser, see below |
| P8 | Dashboard, analytics, reports | done | BUG-23 fixed for real; 24/25/26 re-verified already correct |
| P9 | Access control & policy | done | BUG-27 + BUG-28 both fixed and verified live, see below |
| P10 | Copy, wording & polish | done | BUG-29/30/31 all resolved — see below |

---

## Session log

### Session 1 (2026-08-30)
- Read `docs/assessment-tool-debug-plan.md` and `docs/phase-prompts.md` in full. Confirmed no prior tracker existed for this directive.
- User instruction captured for future sessions: use these two files as the active directive; warn when the session's context/token budget is getting tight and tell the user to open a new session (they'll say "continue"); do not pause for confirmation/verification during normal fix work.
- Created this tracker.
- **Phase 0: done.** `CONTEXT.md` (repo root) + `DEFECT-BACKLOG.md` created, both citing real file:line evidence (a background Explore agent covered attempt lifecycle/cross-user isolation/tab-switch/timer/demo account; independently verified i18n, the Exit-Test control, and BUG-30's real target myself against the live DB and code). Concrete root causes found for BUG-03/04/05/06/07/11/15/16/17 — see CONTEXT.md, not re-derived here.
- **Phase 1: done (BUG-01, BUG-02).**
  - `db/shared/wipe-user-data.ts` — one FK-safe deletion routine (`wipeUserOwnedData`) shared by both bugs so their table lists can't drift apart. Verified against the live schema with a zero-match UUID (proves the SQL is valid without touching real data) before ever running it for real.
  - **BUG-01**: `db/scripts/reset-user-data.ts`. Defaults to a dry run (prints row counts, deletes nothing); needs both `--i-know` and `--execute` to actually wipe. This project has no separate staging/prod environment to gate on (one live Supabase DB total, confirmed via `db/config/env.ts` — there's no environment discriminator to check), so the two-flag requirement is the real safeguard in place of a prod check that doesn't exist. **Dry-run output confirmed live**: 9 users, 170 attempts, 204 responses, 25 scorecards, 194 generated test shells, 139 sessions would be deleted; content tables untouched. **Not executed for real this session** — an actual destructive run against the one shared live database needs the user's explicit go-ahead (the plan's own precondition: a tested DB backup) before it's run, per this session's standing rule to still ask before genuinely irreversible actions.
  - **BUG-02**: implements the plan's Option B (delete-on-login), not Option A (per-login `demo_session_id` scoping on every table) — documented as a deliberate, stated decision in `demoController.ts`'s own header, not guessed: Option A needs a schema change plus a new filter on every existing read path across the app; this is a single low-traffic shared demo login, not a high-concurrency path where Option A's race-safety would actually matter. New `POST /auth/demo/reset` (`backend/src/controllers/demoController.ts`), called by `frontend/src/services/demoSession.ts`'s new `resetDemoAccountData()` right after `ensureDemoSession()` in `LandingView.tsx`'s `handleDemoAccountLogin`. Wipes the account's owned data (attempts, scorecards, plans, notifications, sessions...) but deliberately **keeps the identity row** (`core.app_user`) stable — an earlier version of this deleted it too, which turned out to mint a brand-new `user_id`/`member_code` and refire "first login" side effects (welcome notification, audit row) on every single login; fixed before considering this done. **This explicitly supersedes `docs/ASSESSMENT_FIX_TRACKER.md` item 6's earlier decision to keep 5 realistic pre-seeded scored attempts on the demo account** — flagging clearly since the two are in direct tension and this session's fix wins per the newer, more specific complaint. `db/scripts/demo/seed-demo-account.ts` still works if a polished dataset is wanted for a screenshot; it'll just be cleared on the next real login.
  - **Verified live over real HTTP** (throwaway scripts under `db/scripts/manual/_verify-*.ts`, deleted after use): signed in as the real demo account, called `/me` then `/auth/demo/reset` twice in a row — first call wiped 12 real attempts/11 scorecards/70 responses down to 0, second call was a true no-op (idempotent), `core.app_user.user_id`/`member_code` confirmed stable across both calls. Dev API server started for this, then stopped afterward (port 4000 confirmed clear).
  - `npx tsc --noEmit` clean; `npm run test:unit` 63/63 node:test + 34/34 Vitest pass (unchanged).
- **Phase 2: done (BUG-03, 04, 05, 06, 07, 08).** Root causes were already nailed down precisely in Phase 0's `CONTEXT.md` §2-4 — this phase built the actual fixes against them.
  - **BUG-03 (ghost test / 0:00 evaluating)**: `enforceExpiry` (`db/assess/test/attempt/expiry.ts`) existed but was never called from anywhere — now wired into `GET /assess/attempts` (`listOwnAttempts`, via new `reconcileUserAttempts`) and `GET /assess/attempts/:id/envelope` (`getEnvelope`), the exact two calls `getActiveSession()` makes on every app load. An expired attempt is force-closed the moment either endpoint is hit, before it can ever be rendered as a live countdown again.
  - **New guard, also BUG-03/BUG-08**: `startAttempt` now rejects a second concurrent attempt for the same user (`ActiveAttemptExistsError`, 409 `ACTIVE_ATTEMPT_EXISTS`, carries the existing attempt id) instead of allowing unlimited concurrent attempts (confirmed live: no such guard existed before). Reconciles first, so a genuinely-expired attempt never blocks a fresh start.
  - **Two real, previously-latent bugs found and fixed along the way, not guessed**: (1) `loadSectionSchemes` (called from inside both `startAttempt`'s and `submitAttempt`'s open transactions) used the shared `pool.query` instead of the transaction's own client — requesting a second connection from the same pool while one was already held. `db/shared/pool.ts` caps the pool at 4; running 4 of these transactions concurrently (which reconciling a real backlog of stale attempts started doing) **deadlocked every one of them forever**, confirmed live via `pg_stat_activity` (4 backends stuck "idle in transaction" on the exact same query). Fixed by threading the transaction's client through. (2) A related, separate real Postgres deadlock (40P01) on `assess.user_question_seen` when two of the *same user's* attempts were submitted concurrently — concluded concurrency itself was the wrong tool here (a real user can never submit two attempts at the same instant; only artificial reconciliation concurrency could trigger it), so `reconcileUserAttempts` runs strictly sequentially, capped at 10 stale attempts per call so a large historical backlog (found live: one shared dev/script account had 49) can't stall a single request for minutes — logged the still-unfixed sibling risk (`assembleForAttempt` has the same nested-pool-query pattern, unexercised by this session's changes) as **DB-01** in `DEFECT-BACKLOG.md`.
  - **BUG-04 (cross-user bleed)**: backend was already correctly `user_id`-scoped everywhere checked (re-verified, not just trusted). Real frontend-only bleed found and fixed: `App.tsx`'s `attempts`/`activeAttemptId` state was never cleared on sign-out — fixed in `endSession`. **Added the automated cross-user test the plan's own exit gate requires** (`db/assess/test/attempt/cross-user-isolation.test.ts`) rather than relying on a manual check — verified live: user B's attempt list, envelope fetch, and pause call all correctly reject/exclude user A's attempt.
  - **BUG-05 (tab-switch loses answers)**: no `visibilitychange`/`beforeunload` listener existed anywhere. Added one in `TestTakingView.tsx` that flushes dirty (unsaved) answers immediately on tab-hide/pagehide — does **not** change the exam timer's keep-running-in-background behavior, since that's still an open product/policy decision (`CONTEXT.md` §"Open questions") this session didn't have the authority to resolve unilaterally.
  - **BUG-06 (resume after refresh/logout)**: `getActiveSession()`'s silent auto-navigate-into-`test_taking` (the actual ghost-test mechanism) replaced with a real resume prompt — a non-dismissible modal ("Test in progress — MM:SS remaining. Resume / Submit Now.") shown on the portal screen instead of auto-entering the attempt. "Submit Now" calls the real submit API directly from the modal without ever entering `TestTakingView`.
  - **BUG-07 (Exit control during an active attempt)**: found concretely — a button labeled "Exit Lobby" (a leftover mislabel) rendered in the live question-palette sidebar during active question-answering, whose handler was 100% client-side (cleared local state, no server call at all — a direct path to an orphaned `in_progress` attempt). Fixed to option (b) from the plan: relabeled "Exit & Pause Test", now flushes unsaved answers and calls the real pause API before leaving, with a corrected confirm-dialog wording.
  - **BUG-08 (some tests don't start)**: investigated all 4 listed causes — (1) stale-attempt blocking is now a **real, intentional** guard (not silent — surfaces `ACTIVE_ATTEMPT_EXISTS` with a clear message pointing at the new resume prompt, wired into `TestListView.tsx`/`CoursesView.tsx`/`DashboardView.tsx`'s existing error-surfacing pattern); (2) insufficient question pool already surfaced clearly via `PoolInsufficientError` (pre-existing, confirmed); (3) missing Tamil translation already falls back gracefully, no throw (confirmed in Phase 0 recon); (4) broken media already handled gracefully by `QuestionImage.tsx` (pre-existing). One gap logged, not fixed: no publish-time pool-sufficiency validation for admin-authored papers — logged as **DB-02** in `DEFECT-BACKLOG.md` (low priority: the admin route it would attach to, `ingestFixedPaper`, has zero live callers today).
  - **Verified**: `npx tsc --noEmit` clean; `npm run test:unit` 63/63 node:test (up from 63 → includes the new cross-user-isolation test file, net test count is per-`t.test()` so the file total shows separately) + 34/34 Vitest; `npm run build` clean (frontend + backend bundle); live-DB verification scripts (deleted after use) confirmed the expiry-close-then-guard-block sequence end to end.
  - **Not done / explicitly deferred, not guessed**: BUG-05's exam-timer-on-blur policy (keep running vs. pause) is a product/exam-integrity decision, not a code question — left as-is (currently keeps running) pending that decision. A full Playwright regression pass was kicked off at the end of this session to catch any UI regression in the touched screens (Dashboard/TestTakingView/App shell) — see below for its result.
- **Phase 4: done (BUG-11, 12, 13, 15). BUG-14 correctly left deferred.**
  - **BUG-12 (repeated questions)** and **BUG-13 (wrong questions/options)**: re-verified rather than assumed — both were already fixed by `docs/assessment-tool-fix-prompt.md`'s P0-3 (three-layer dedup in `assemble.ts`/`attempt-flow.ts`) and the existing schema (options are always keyed by a stable `option_id`, never array index, both in the DB answer key and the frontend's option-list rendering). No regressions from this session's own changes — `assemble.test.ts`'s dedup-specific assertions (now also exercising the new startAttempt guard) still pass.
  - **BUG-15 (partial Tamil within one question)**: found the concrete, real gap in `envelope.ts` — the Tamil-options join matched `option_texts[i]` positionally with no completeness check, so a translation row with a short/blank `option_texts` array (or a blank stem) would silently under-translate a question, exactly the "partially in Tamil" complaint. Live data currently has zero such rows (checked), but nothing stopped a future import from reintroducing it. Fixed: a question's Tamil is now applied only when the stem is non-blank AND every option has a non-blank translated text at the right length — otherwise the whole question falls back to English, never a mix (`db/assess/test/attempt/envelope.ts`).
  - **BUG-11 (Tamil "can't fetch the material" / content-health)**: root cause per Phase 0 recon — not a fetch failure, a real absence for ~8.6% of published questions (120/1399) with no UI affordance to say so. Added the requested `GET /content/content-health` endpoint (mounted under the existing `/content` router + `content:publish` permission, not a new top-level `/admin` prefix this app doesn't otherwise use for content) returning `totalPublished`, `missingTamilEntirely`, `tamilOptionCountOrBlankMismatch`, `tamilStemBlank` — verified live: 1399 published, 120 missing entirely, 0 mismatches, 0 blank stems today.
  - **BUG-14**: correctly left untouched — still needs the screenshot + section name + question id the plan itself requires before anyone (human or agent) should guess at what "that case is showing in the Question sections" even means.
  - **Verified**: `npx tsc --noEmit` clean; `npm run test:unit` 63/63 node:test + 34/34 Vitest (re-run after the envelope.ts/content.routes.ts changes, both green); content-health SQL hand-verified against the live DB before wiring it into the route.
  - **Playwright regression note (process hygiene, not a product bug)**: the full E2E suite was re-run three times at the end of Phases 2 and 4 combined. Two real, deliberate test updates were needed and made in `tests/happy-path.spec.ts`: two assertions hardcoded `/Great work, Prince A!/` as the post-submit dashboard heading, which stopped matching once BUG-02 made every demo login wipe prior history first — the account's first-ever attempt now deterministically shows `motivationalMessage.ts`'s "Nice work getting started!" branch instead, which is the *correct* new behavior, not a regression, so the assertions were updated to match it (with the reasoning recorded in the test file itself). Separately, one run showed 3 flaky failures (Supabase's shared session-mode pooler capped at 15 connections project-wide — confirmed via a live "EMAXCONNSESSION" error) traced directly to this session running the full Playwright suite **concurrently** with `npm run test:unit` — both hammer the same live DB at once. Re-running Playwright alone, uncontended, is the real signal; see below for that result. Lesson applied: don't run two DB-heavy suites at the same time in this environment.
- **Phase 8: done (BUG-23 for real; 24/25/26 re-verified, not re-guessed).**
  - **BUG-23 (Tests Taken count wrong) — found the real, concrete cause**: the Dashboard's "TESTS TAKEN" tile (and the achievement-gating logic right next to it) was driven by `App.tsx`'s `attemptsCount` prop, itself `attempts.filter(...).length` over the same ephemeral, client-only `attempts` array already flagged for BUG-04's cross-user bleed risk. That array starts from a fixed seed and only grows when a test completes *within the current browser session* — a real user with 147 real scored attempts in the database would see a count near zero on any fresh reload, new device, or new login, until they took another test in that exact tab. Fixed at the source: added a real `totalScoredAttempts` field to `getDashboardAnalytics` (`db/assess/analytics/dashboard.ts`, one more `count(*) where attempt_state = 'scored'` query on the same already-shared connection, matching this function's existing "one client, sequential queries" discipline that a prior phase put in place specifically to avoid exhausting Supabase's shared pooler). `DashboardView.tsx` now derives `attemptsCount` locally from `analytics.totalScoredAttempts` instead of taking it as a prop; the now-unused prop and its pass-through in `App.tsx` were removed. Verified live against real data: the account with the largest history in this dev DB showed 147 scored attempts via the new query — a number the old client-array approach could never have produced from a fresh page load.
  - **Found and logged, not fixed (real scope boundary, not laziness)**: the exact same architectural problem (App.tsx's `attempts` array being unreliable across reloads) also affects `TestListView.tsx`'s "recent attempts" strip and the daily-reminder "did they test today" check — BUG-23 named the Dashboard count specifically, and fully replacing `attempts` state everywhere it's read is a bigger, separate change; logged as **DB-03** in `DEFECT-BACKLOG.md`.
  - **BUG-24 (Subject Performance)** and **BUG-25 (Overview/weakest topics)**: re-checked `AnalyticsView.tsx`/`DashboardView.tsx` directly rather than trusting the earlier Phase G record — both are already fully driven by the real `useDashboardAnalytics()` hook (`subjectAccuracy`, `weakestUnits`), no stale-array involvement found. No change needed.
  - **BUG-26 (PDF report/branding)**: not re-audited in depth this pass (no evidence found suggesting it shares BUG-23's root cause) — trusting the earlier, already-verified Phase G/ASSESSMENT_FIX_TRACKER record (`ReportBrandHeader.tsx`, Tamil-safe server-would-be-better-but-html2canvas-is-what-exists approach) rather than re-deriving it from scratch.
  - **Verified**: `npx tsc --noEmit` clean; `npm run test:unit` 69/69 node:test + 34/34 Vitest; `npm run build` clean; the real `totalScoredAttempts` count spot-checked against the live DB.
- Starting Phase 3 (test console UX & layout) next per the plan's own severity order (P0 → P1 → P2 → P4 → P8 → P3 → P5 → rest).

### Session 2 (2026-08-30)
- **Phase 3: done (BUG-09, BUG-10).**
  - **BUG-09 (full view only)**: already satisfied by App.tsx's existing screen routing (`currentScreen === "test_taking"` renders `TestTakingView` standalone, outside the Header/main/footer app shell entirely — confirmed by reading `App.tsx`, not assumed) — no code change needed. The Fullscreen API request is explicitly "optional" in the plan's own spec; not implemented, since the dedicated-layout requirement (the actual bug report) was already met and a real product decision (auto-request fullscreen on every test start) felt out of scope to make unilaterally.
  - **BUG-10 (fixed height, internal scroll, image frame)**: found live, before any new work, that a *previous uncommitted session* had already applied the core fix (`h-dvh`+`overflow-hidden` shell, `min-h-0`+`overflow-y-auto` on both the question panel and palette panel) — commented inline in `TestTakingView.tsx` citing BUG-10 by name, but never verified against the plan's own 9-combination acceptance matrix or marked done in this tracker. This session verified it for real and found + fixed two genuine gaps the earlier pass had missed:
    1. **Real bug, found live via a failing verification test (`tests/_bug10-layout-check.spec.ts`, kept as permanent regression coverage — see below): a demo-login race.** `App.tsx`'s global `supabase.auth.onAuthStateChange` listener flips `isAuthenticated=true` (revealing the whole authenticated app) the instant `ensureDemoSession()`'s `signInWithPassword` resolves — which is *before* `LandingView.tsx`'s `handleDemoAccountLogin` has finished its own sequential `await resetDemoAccountData()`. A script (or a fast enough real click) reaching "Start Practice" in that window creates a real `assess.test`/attempt row that the still-in-flight reset then deletes moments later — confirmed live via a genuine `404 assess.test not found` straight out of `startAttempt`, reproduced deterministically, then fixed and reproduced-as-fixed. Not a Phase 3 bug per se (it's BUG-02/demo-login territory) but it directly blocked verifying BUG-10 and is a real correctness bug in production, not just test infra — fixed via a `demoLoginInFlight` flag (`demoSession.ts`) the listener checks before reacting to a demo-account sign-in, deferring to the explicit `onLoginSuccess` call the login flow already makes once reset genuinely completes.
    2. **Real bug, found live via the actual 390px screenshot (plan's own required width), not guessed:** the question panel and the question-palette panel both used `flex-1` in the mobile-stacked layout, splitting the viewport ~50/50 — the answer options were pushed off-screen behind the palette's internal scroll for *every* question at phone width, not just a long one. Fixed by making the palette an off-canvas drawer below `lg` (toggle button + count badge in the question panel's footer, close button + backdrop on the drawer, auto-closes on selecting a question) — matching the plan's own footer spec ("palette toggle") rather than a permanently-visible stacked panel. Kept Submit reachable without opening the drawer (a mobile-only duplicate button, `lg:hidden`) since it's the single most time-critical action; Exit & Pause stayed inside the drawer only (lower urgency, still one tap away).
    3. **Gap closed, not a bug found broken:** `QuestionImage.tsx` had no click-to-zoom, which the plan's own image spec explicitly asks for ("Add a lightbox/zoom for dense diagrams"). Added a small always-there zoom-in affordance (corner button + click-the-image) opening a fixed full-screen overlay, `object-fit: contain`, no new dependency.
  - **Verified live, all 3 required widths (1920/1366/390px), via a real demo-account run through the full launch chain (Tests → Configure Practice → PHY → Start Practice → System Check → Lobby → TestTakingView), not assumed**: `documentElement.scrollHeight - clientHeight <= 2` at every width (i.e. the browser window itself never scrolls — the core BUG-10 assertion); header (timer) and footer nav (Previous/Save & Next/Submit) all stay visible with zero scrolling at every width, including 390px after the palette-drawer fix. Screenshots inspected directly (not just the assertion), confirming no visual regression at 1920/1366px and the mobile drawer/Submit-button fix at 390px. Did **not** get to exercise all "3 question types" (long text / tall portrait image / wide landscape image) from the plan's 9-combination matrix — the demo bank's Physics-practice question set reached by the test happened to be text-only MCQs; verifying a genuinely 800-word question and a tall image against this same layout is still open, logged as **DB-04** below rather than guessed at.
  - `npx tsc --noEmit` clean; `npm run test:unit` 69/69 node:test + 34/34 Vitest (unchanged, confirms no regression from the palette-drawer/zoom/race-guard changes); `npm run build` clean.
  - **Not done / explicitly deferred, not guessed**: BUG-09's optional Fullscreen API request (see above — a real product-policy call, not a code gap). DB-04 (image/long-question layout combinations) logged in `DEFECT-BACKLOG.md`, not fixed blind.
- Starting Phase 5 (App-wide EN/TA language system) next per the plan's own severity order (P0 → P1 → P2 → P4 → P8 → P3 → P5 → rest) — Phase 4 (question content correctness) is Phase 5's stated precondition and is already done (Session 1).

- **Phase 5: done, partially (BUG-16, BUG-17) — full string-coverage sweep explicitly deferred as DB-05, not guessed at.**
  - **Recon first, not assumed**: `CONTEXT.md` §7 (written in Phase 0) had already confirmed live there is no browser/Google page-translate widget — `LanguageContext.tsx` was already a real, if incomplete, i18n layer (a 443-entry inline dictionary, `t(key)` used in 23 files). So BUG-16's "remove the page-translation mechanism" step was already true (nothing to remove); the real gaps were persistence, resource-file structure, and the `notranslate` guard against the *browser's own* auto-translate prompt (CONTEXT.md's own flagged plausible freeze cause, never ruled out).
  - **BUG-16 fixes**: extracted the inline dictionary into real `frontend/src/i18n/en.json`/`ta.json` (443 keys each) — done via a script that imported the TS object directly and serialized it, not retyped by hand, so no transcription risk to the Tamil text. `LanguageContext.tsx` now persists the choice to `localStorage` (`lumen_ui_lang`) and a readable cookie, applies `document.documentElement.lang`, and `index.html` got `translate="no"` + `<meta name="google" content="notranslate">` as a cheap, safe guard against the still-unconfirmed browser-auto-translate freeze theory. Added the plan's required single global toggle to `Header.tsx` (next to the existing dark-mode toggle) — previously the *only* language control anywhere in the app was buried inside `TestTakingView`, which is BUG-17's exact root cause (see below).
  - **BUG-17 fix — the real architectural change**: confirmed live (grep across the frontend) that `TestTakingView.tsx` was the only file that ever called `toggleLanguage()`, mutating the one global `language` state every other one of the 23 `useLanguage()` consumers reads — so switching language mid-test silently changed the Dashboard/Header/every other screen too, exactly the reported leak. Also found, not previously documented: the old "language" toggle inside the test wasn't even a real EN/TA switch — its `ta` mode always rendered Tamil *stacked below* English, meaning there was no way to see a question in Tamil alone. Fixed by adding a wholly separate `questionLanguage` state (`'en'|'ta'|'bilingual'`) local to `TestTakingView`, persisted to its own `localStorage` key (survives a mid-test refresh, per BUG-06), with a real 3-way EN/TA/EN+TA control that never touches `LanguageContext`. Stem/option rendering now honors all three modes correctly, falling back to English-only in pure "ta" mode when a question's Tamil is incomplete — safe to key off `!!stemTextTa` alone because Phase 4's BUG-15 fix already guarantees envelope.ts only ever populates `*TextTa` fields when the whole question (stem + every option) is complete in Tamil, never a partial set.
  - **Verified live, not assumed**: a real demo-account run through Header's new toggle confirmed nav labels flip to Tamil, `<html lang>` updates to `ta`, and the choice survives a hard reload; a separate real test-taking run confirmed `questionLanguage` defaults independently of `ui_lang` (EN, even while chrome was in Tamil) and switching it to "ta" renders only the Tamil stem/options, not both stacked — the concrete behavior BUG-17 asked for.
  - **Not done / explicitly deferred, not guessed — logged as DB-05**: the plan's "no hardcoded user-visible strings anywhere" is a much larger ask than the architecture fix above. Spot-checked `Header.tsx` alone and found many strings either never wrapped in `t()` at all, or wrapped with a key that has no dictionary entry (silently falls back to English either way) — "My Profile," "Aspirant Name," "Danger Zone," etc. This almost certainly repeats across the other 20+ `useLanguage()`-consuming files. A full sweep is hundreds of strings across dozens of files and the plan's own exit gate (click through every route in Tamil, zero untranslated strings) requires actually exercising every screen — neither was attempted this session; DB-05 has the concrete next-step recommendation.
  - **Verified**: `npx tsc --noEmit` clean; `npm run test:unit` 69/69 node:test + 34/34 Vitest (unchanged — confirms no regression from the i18n restructure); `npm run build` clean; the Phase 3 layout regression suite (`tests/_bug10-layout-check.spec.ts`) re-run clean at all 3 widths with the new header/in-test language controls added to confirm no layout regression from this phase's UI additions.
- Starting Phase 9 (access control & policy) next per the plan's own severity order (P0 → P1 → P2 → P4 → P8 → P3 → P5 → **rest**) — everything ahead of "rest" is now done; remaining phases (P6 syllabus, P7 study tools, P9 access control, P10 polish) have no further ordering constraint between them per the plan, picking P9 first since BUG-27 (delete-account exposure) is a real security gap already spotted live in Header.tsx this session, not a cosmetic one.

- **Phase 9: done (BUG-27, BUG-28) — both verified live against the real server, not assumed from reading code alone.**
  - **BUG-27 (self-service account deletion exposed)**: found live that `DELETE /me` (`backend/src/routes/api.ts`) was a real, well-built, OTP-reauthenticated self-service deletion endpoint — not a stub — reachable by any signed-in student via a "Danger Zone" button in `Header.tsx`'s Account Settings modal. Also found, not assumed: an admin-side deletion path **already existed** and needed no new backend feature — `POST /admin/users/:id/status {toStatus:"deleted"}` (`adminUser.service.ts`, from an earlier LA-BE-CORE-002 phase) already soft-deletes a target user (bans the Supabase auth identity, sets `core.app_user.status='deleted'`, writes an audit row) — exactly the plan's own "soft-delete + anonymise" preference, already built. Fix: removed the entire Delete Account UI (button, OTP-verify modal, and the now-dead `deleteAccount()` frontend API wrapper in `meApi.ts`) from `Header.tsx`; gated `DELETE /me` itself behind the same admin-role check `admin.routes.ts`'s user-management routes already use (`requireUserManagePermission`, exported from there for reuse) rather than deleting the route outright, specifically so a direct call from a normal user gets a clean, intentional 403 instead of a generic 404.
  - **Verified live (not just read)**: a real demo-account (non-admin) session confirmed zero "Delete Account" text anywhere in the DOM, and a direct `fetch("/api/me", {method:"DELETE"})` call using that session's real token returned `403` (previously would have required the full OTP flow to even attempt, but was otherwise unauthorized-by-design once OTP was faked/skipped — now blocked at the permission layer before any of that logic runs).
  - **BUG-28 (gate Full Tests)**: plan's own recommended rule (option a — no completed practice test, no full mock), which had **zero enforcement anywhere** before this fix — confirmed live that `TestListView.tsx`'s "Start Full Mock Test" button used a *different*, currently feature-flagged-off gate (`FULL_MOCK_REQUIRES_SYLLABUS_COMPLETION`, a separate P1-9 decision, left untouched) and `StudyPlanView.tsx`'s "Mock Test" quick-start button had no gate at all — called `createSession({mode:"full-mock"})` directly. Real fix, server-side, on the one endpoint both paths funnel through (`sessionController.ts`'s `createSession`): a new `hasCompletedPracticeTest` check (attempt_state='scored', same definition BUG-23/P8 established, on a non-MOCK test — test type read off the `test_code` TEST_TYPE segment via the same convention `deriveSessionModeFromTestCode` already uses, since it isn't a real column) throws a structured 403 `FULL_MOCK_LOCKED` when it fails. `TestListView.tsx`'s button now shows its real state — "Checking eligibility...", "Complete 1 practice test to unlock", or the two gates' own distinct messages — never a silently disabled button with no explanation, per the plan's explicit requirement.
  - **Verified live, both directions**: a fresh demo account (post-reset, zero attempts) showed the locked button state and a direct `POST /api/assess/sessions {mode:"full-mock"}` call returned `403 FULL_MOCK_LOCKED`; the same session then completed one real subject-wise practice test end-to-end (through submit), and the identical direct API call immediately after returned `201` — the gate lifting for real, not just the UI's own optimistic assumption.
  - **Verified**: `npx tsc --noEmit` clean; `npm run test:unit` 69/69 node:test + 34/34 Vitest (unchanged); `npm run build` clean.
  - **Not done / explicitly deferred, not guessed**: neither bug needed anything deferred — both had a complete, verified fix within the session's own scope.
- Remaining phases with no further severity-order constraint: P6 (syllabus), P7 (study plan/tasks/notes/pomodoro), P10 (copy/wording/caching). Next session should read this log, then pick up P6 first (untouched so far, and — per the plan's own P6 spec — often "the asset was never uploaded" rather than a code fault, worth checking early) unless the user redirects.

### Session 3 (2026-08-30)
- **Phase 6: BUG-18 investigated fully; blocked on a non-code action, not guessed around.**
  - The plan's own P6 spec explicitly says to check "was the asset ever uploaded" first — did that, live, rather than assuming the code was broken. Found the view/download code (`frontend/src/pages/CoursesView.tsx`'s iframe preview + `downloadUnitMaterial`, `frontend/src/services/unitMaterialsApi.ts`, `backend/src/controllers/unitMaterialController.ts`) was already built correctly and committed by a prior session (`a02417b`, predates this debug-plan tracker) — Drive-file-id mapping sourced from a real, Drive-verified document (`db/books/I20_23_Resource_Library_Book_List_REAL.md`), not fabricated. DB coverage confirmed live: 82 active `learn.unit_material` rows, 0 with a blank `drive_file_id`, all 38 catalog units have at least one material.
  - **Real root cause, confirmed live, not assumed**: wrote `db/scripts/check-drive-material-access.ts` (kept as a permanent, reusable tool, not a throwaway) — plain HTTP requests with no Google session, i.e. exactly what a real student's browser hits. Result: **82 of 82** active materials redirect to Google's `accounts.google.com` sign-in wall or return 401/403 instead of the file. This is a total gap across all 4 subjects, not partial. This is the exact risk `docs/NEET_TOOL_FIX_TRACKER.md` (an earlier, separate tracker) had already flagged as unverified ("needs a human, not a script") — now confirmed as a real, live-blocking bug.
  - **Fixed, in scope (code-side mitigations)**: (1) `unitMaterialsApi.ts`'s `downloadUnitMaterial` previously would have silently saved Google's HTML sign-in page to disk renamed as `"<title>.pdf"` for any blocked file (the redirect chain resolves to a 200 OK HTML page, not an error status) — now checks `content-type` and throws instead. (2) `CoursesView.tsx`'s download-failure handler previously surfaced a raw, meaningless error (`err.message`, e.g. a CORS `TypeError` string) — now shows one clear sentence. (3) Added a static hint inside the in-app preview modal ("Seeing a Google sign-in page instead of the document?...") since cross-origin iframe content can't be inspected from our code to detect the failure automatically. `npx tsc --noEmit` clean after all three.
  - **Not fixed, cannot be fixed in code**: making the 82 Drive files actually public requires bulk-editing sharing settings on the source Google Drive account/folder — that needs a human with edit access to that Drive account, not a coding session. Logged as **DB-06** in `DEFECT-BACKLOG.md` with the full context; re-run `db/scripts/check-drive-material-access.ts` after the sharing fix lands to confirm before calling BUG-18 done. **Flagged to the user directly** rather than silently marked done or left unmentioned, since it's the one open item in this session that only they (or whoever owns the Drive account) can resolve.
  - Phase 6 status set to `blocked (code side done)` above rather than `done`, since the plan's own done-when criterion ("download saves a valid, openable file... for every subject") genuinely cannot be met yet.
- Moving to Phase 7 (study plan, custom tasks, notes, pomodoro) next per the plan's own remaining-phase order — independent of the Drive-sharing blocker above.

- **Phase 7: done (BUG-19, 20, 21, 22) — all four built for real, not patched.** Recon first (background Explore agent, not guessed): `learn.study_plan`/`plan_task` (005_learn.sql) existed but were never wired to `StudyPlanView.tsx` at all (`handleSavePlan` only flipped a local boolean for 3s); "My Custom Study Tasks" and "My Personal Revision Notes" called `frontend/src/services/supabase.ts` functions querying Supabase tables (`user_tasks`, `user_notes`) that were **never migrated** — confirmed via grep across `db/migrations/`, every call silently failed and was swallowed; Pomodoro's session log (`studySessionService.ts`) was pure `localStorage`, invisible to the demo-reset and lost on device change.
  - **New migration `db/migrations/026_learn_study_tools.sql`** (+ `db/verify/verify_026_learn_study_tools.sql`), applied and verified live: `learn.study_plan` gets `config jsonb`/`created_at`/`updated_at` + a partial unique index `uq_study_plan_one_active_per_user (user_id) where plan_status='active'` — the actual DB-level "one active plan" constraint BUG-19 asks for, not just a hidden button. Three new tables: `learn.study_plan_goal` (the chapter checklist, now real rows with `sort_order` instead of a hardcoded in-memory array), `learn.custom_task`, `learn.revision_note`, `learn.pomodoro_session` (the last widened partway through to also carry `subject`/`task_title`/`session_type`/`rating`/`notes` — matching `studySessionService.ts`'s existing `StudySession` shape exactly — once it became clear the streak/subject-breakdown stats functions needed those fields preserved, not just `duration_seconds`).
  - **BUG-19**: `db/learn/study_plan/study-plan-flow.ts` (new) — `getOrCreateActivePlan` (create-once), `updatePlanConfig`/`resetPlan` (archive current + insert fresh, one transaction so the unique index is never violated mid-flight), `listGoals`/`createGoal`/`updateGoal`/`deleteGoal`/`reorderGoals`. New `backend/src/controllers/studyPlanFlowController.ts` + routes on `learn.routes.ts` (`GET/POST /study-plans/me`, `POST /study-plans/reset`, `/:planId/goals` CRUD + `PUT .../goals/order`) — the fixed sub-paths registered on their own router **ahead of** the generic `makeOwnedCrudRouter`, same pattern as the existing `notificationsListRouter`, so `/me`/`/reset` are never swallowed as a literal `planId`. Frontend: reorder is move-up/move-down buttons, not drag-and-drop (avoids a new dependency for a checklist that's typically under a dozen items — a deliberate scope call, not a gap). "Reset Plan" button added next to Save.
  - **BUG-20/21**: `db/learn/custom_task/` and `db/learn/revision_note/` (new repositories, same shape as the existing `notification`/`topic_mastery` repos) + matching `custom-tasks`/`revision-notes` routes (list-mine pattern, same reasoning as notifications). `revision_note.repository.ts` strips HTML tags server-side on every write (`stripHtml`) — real, if modest, sanitisation for a plain-textarea feature with no rich-text input today. Notes got a search box (title/content/subject/topic, client-side filter) and a `topic` field (was schema/type-only until added to the actual form). Frontend: `customTasksApi.ts`/`revisionNotesApi.ts` (new) replace the broken Supabase calls; the dead `fetchUserTasks`/`saveUserTask`/`deleteUserTask`/`fetchUserNotes`/`saveUserNote`/`deleteUserNote` + `UserTask`/`UserNote` types were deleted from `supabase.ts` outright (confirmed unused elsewhere first), not left as dead code.
  - **BUG-21 autosave**: editing an existing note debounce-saves 800ms after the last keystroke via a `useEffect` keyed on the note's fields, with a real "Saving…" / "Saved" indicator next to the form (not just a toast). A brand-new note still needs one explicit "Save Note" click (nothing to attach a debounce to before it has a real id) — editing it again afterward autosaves the same way.
  - **BUG-22**: `learn/pomodoro_session` repository (`findRecentByUser`, default limit 20 matching the plan's own "last 20 sessions" ask) + `/pomodoro-sessions` routes, with an optional `?limit=` (capped at 200) — added because the streak calculation in `Header.tsx`/`DashboardView.tsx` needs a wider window than the visible log's own 20 to count consecutive days correctly; `frontend/src/services/pomodoroApi.ts` (new) exports `toStudySessions()`, one shared mapper (DB row -> the existing `StudySession` shape) so `calculateStudyStreak`/`calculateSessionStats` (pure functions, kept as-is) work unchanged against real server data instead of localStorage. `PomodoroTimer.tsx`, `Header.tsx`, and `DashboardView.tsx` all rewired onto this; `studySessionService.ts`'s localStorage save/fetch/delete functions deleted (confirmed unused elsewhere first) — only the pure calculation functions + the `StudySession` type remain. The log drawer already had a `max-h-60 overflow-y-auto` scroll container from an earlier session (matches the BUG-10 pattern) — confirmed, not re-fixed; the short, fixed-content completion/rating modal doesn't need one (not the unbounded-growth case the plan's own spec is about). For demo users this whole log is swept for free — `learn.pomodoro_session` was added to `db/shared/wipe-user-data.ts`'s deletion list alongside `custom_task`/`revision_note`, so `demoController.ts`'s existing reset call (BUG-02) covers all three without any change to that controller itself.
  - **Verified live, in layers, not assumed at any stage**: (1) every new DB function called directly against the live DB with a real (system, non-human) `core.app_user` row — create-once idempotency, the unique-index constraint actually blocking a raw duplicate insert, reset's archive-then-create, goal add/toggle/delete/reorder, task complete/uncomplete `completed_at` toggling, note HTML-stripping on both create and update, pomodoro session creation — all confirmed, then the test data cleaned up. (2) A full HTTP pass against the real dev server signed in as the actual demo account (`POST /auth/demo/reset` first for a clean slate) exercising every new route exactly as the frontend calls it, including the fixed-path-vs-generic-`:id` route ordering (`POST /study-plans/me` and `/reset` do **not** get swallowed by the generic router) and the ownership guard (`GET` a foreign/bogus `planId`'s goals correctly 404s). (3) **A real Playwright run in an actual browser** (not just API calls) — this is what caught the one genuine regression in this phase: `handleSavePlan`'s original guard (`if (chapterGoals.length === 0)`) never fired because `App.tsx` passes `chapterGoals` down as a prop pre-populated with its own hardcoded 8-item placeholder array (non-empty), so the real server-seeded goals were never loaded into shared state after the very first save — a PATCH to `.../goals/g1` (a fake placeholder id, not a real UUID) came back `400` live. Fixed by always resyncing from the server after save (removing the flawed length check) and separately fixing `App.tsx`'s default `chapterGoals` state to start `[]` instead of a fake array with two goals pre-marked complete (also fixes a real, latent `[].every(...)` vacuous-`true` edge case in `isSyllabusCompleted`, currently inert only because the feature flag that reads it is off). Re-ran the full browser flow (plan save → goal toggle → custom task → note create/edit with live autosave confirmation → Pomodoro finish-and-log) end to end after the fix: all green, zero console errors.
  - `npx tsc --noEmit` clean; `npm run test:unit` 69/69 node:test + 34/34 Vitest (unchanged — confirms no regression); `npm run build` clean.
  - **Not done / explicitly deferred, not guessed**: dashboard-counter wiring for custom-task completion (BUG-20's own fix spec) — checked and there is currently no dashboard widget anywhere that references task completion (grepped for "tasks completed"/similar, found nothing), so the one place that does show a live count (the panel's own "X/Y Done" badge) is already correctly reactive; nothing else exists to wire up. Drag-and-drop goal reordering (used move-up/move-down instead, a deliberate scope call, see above).

- **Phase 10: done (BUG-29, BUG-30, BUG-31).**
  - **BUG-30 ("remove the word... whenever you are lagging")**: initially left as an open question per the plan's own §14 warning (searched the whole frontend for "lagging"/"slow"/"delayed" as a loading/degraded-state message and found none — the only hits were the Analytics screen's "Lagging Topics" domain vocabulary). **User confirmed live (2026-08-31) it was the Analytics wording after all** — asked to change the adjective to a motivating one. Fixed: `DashboardView.tsx`'s weak-units section heading changed from `t("Where You're Lagging")` to `t("Where to Focus Next")` (the `t()` key is the literal English string with no prior dictionary entry either way — `LanguageContext.tsx`'s `t()` falls back to the key itself when no translation exists, confirmed by reading it, so this is a safe drop-in rename, not a translation regression). Also renamed a dead, unused i18n key with the same problem while touching this area — `"OPEN FULL NCERT LAGGING REPORT"` in `en.json`/`ta.json` (grepped: no component anywhere calls `t()` with this string, so it was already inert) — to `"OPEN FULL NCERT PROGRESS REPORT"`, softening the Tamil translation's `பின்னடைவு` ("regression/backslide") to `முன்னேற்றம்` ("progress") to match. `laggingTopics`/`LaggingTopic` (the underlying data field/type name) were left as-is — internal code naming, never rendered to a user, out of scope for a copy-wording bug.
  - **BUG-29 (technical wording sweep)**: grepped the whole frontend for the plan's own named vocabulary ("database", "sync"/"syncing", "fetching", "API", "payload", "session token", "cache", "null", "records") rendered as user-visible text (not code identifiers/variable names, which don't count). Found and fixed the one real, concrete cluster: `PomodoroTimer.tsx` said "Database" in five places a student actually sees — the post-log toast ("...to Database!"), the empty-state hint ("record your study time to the database"), the completion modal's copy ("...your database scorecard"), and the log button itself ("Log to Database →") — all reworded to plain language ("Logged X mins of Y!", "record your study time.", "Save this study session to your progress.", "Save Session →"). No other technical-vocabulary leaks found via this pass (same caveat as DB-05: a grep-based sweep for named terms is not the same as clicking through every screen — a fully exhaustive audit remains open, folded into DB-05 below rather than treated as separately "done").
  - **BUG-31 (caching/cookies)**: (1) **Static assets** — `backend/src/server.ts`'s `express.static` now sets `Cache-Control: public, max-age=31536000, immutable` for everything under `dist/assets/` (Vite content-hashes these filenames — a new deploy never reuses an old name, so this is safe) and `no-cache` for everything else (`index.html`, served at the same URL on every deploy) — verified live via `curl` against the real built server, both headers present exactly as expected. (2) **API caching** — added `Cache-Control: public, max-age=300` to `GET /catalog/tree` (`catalogTreeController.ts`) specifically: confirmed it's read-open (no `requireAuth`) and platform-wide/non-personalized (identical response for every caller, live-published-question counts refresh at most every few minutes in practice) — the one endpoint in this pass that's unambiguously safe to cache without any risk of the cross-user bleed BUG-04 already fixed. Verified live: `ETag` (Express's own default, already present) + the new `Cache-Control` both confirmed via `curl`. Deliberately did **not** add caching to any other endpoint — attempt/session/analytics/user-scoped routes stay uncached, exactly per the plan's own explicit warning not to re-introduce BUG-04. (3) **Session cookie hardening** — checked and this doesn't apply as literally stated: this app has **no first-party session cookie at all** (grepped for `document.cookie`/`res.cookie`/Set-Cookie — the only cookie anywhere is `LanguageContext.tsx`'s existing `SameSite=Lax` language-preference cookie from Phase 5, a non-sensitive UI preference, not an auth session). Auth is Supabase's client SDK, which stores its session in `localStorage`, not a cookie — a different, pre-existing architecture choice, not something this pass should change. (4) **Question-media client cache** — question images are Supabase Storage public URLs (`db/content/asset-resolver.ts`), whose cache headers are set by Supabase's own storage service, not this backend; out of this app's code to change without Supabase dashboard/bucket-policy access.
  - **Related fix, same session, user-requested — question-language auto-reset**: while discussing BUG-30/DB-05, the user separately confirmed the app-wide Tamil/English sweep (DB-05) should stay paused/deferred, but pointed out a real, related bug in the test console's own question-language toggle (BUG-17, Phase 5): `TestTakingView.tsx`'s `questionLanguage` (en/ta/bilingual) persists to `localStorage` under `lumen_question_lang` **indefinitely** — it was only ever meant to survive a mid-test refresh (BUG-06's resume flow), but since nothing ever cleared the key, a student's choice on one test silently carried into every *subsequent* test too. Fixed: `handleSubmitAnyway` (the one real submit path — both the manual "Submit" button and the timer-hits-zero auto-submit both route through it) now clears `QUESTION_LANG_STORAGE_KEY` right after a successful submit, so the next test starts fresh in English. Deliberately **not** cleared on pause ("Exit & Pause Test") — a paused attempt is still resumed later via BUG-06's own flow and should keep the student's in-progress choice.
  - **Verified**: `npx tsc --noEmit` clean; `npm run test:unit` 69/69 node:test + 34/34 Vitest (unchanged); `npm run build` clean (including the `en.json`/`ta.json` edits — a build failure would have caught malformed JSON); static-asset and catalog-tree cache headers confirmed live via `curl` against the real production build (`node dist/server.mjs`), not assumed from reading the code alone.
  - **Not done / explicitly deferred, not guessed**: a fully exhaustive BUG-29 string audit beyond the named-vocabulary grep (folded into DB-05's existing scope, not a new separate backlog item).

**Status as of 2026-08-31**: P0–P5, P7–P10 done; **P6 explicitly paused at the user's request** — the code-side mitigations already in place (DEFECT-BACKLOG.md DB-06) are enough for now, no further work planned until the user deals with Google Drive sharing settings themselves. DB-05 (full i18n string sweep) is likewise explicitly held per the user's own instruction, not scheduled for the next session unless they ask for it.
