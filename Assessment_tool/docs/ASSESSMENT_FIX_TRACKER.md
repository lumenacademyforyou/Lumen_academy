# ASSESSMENT_FIX_TRACKER — running tracker for docs/assessment-tool-fix-prompt.md

Source directive: `docs/assessment-tool-fix-prompt.md` (P0 blocking bugs → P1 missing features → P2 polish).
This file is the single source of truth for progress across sessions on that directive. Update status/evidence as work lands; do not rewrite history.

**If the user says "continue" (or similar) in a session on this repo for this directive:** read this file's Session log (bottom) first, re-verify anything marked "in progress", then resume in P0→P1→P2 order without re-deriving what's already recorded here.

Status values: `todo`, `in-progress`, `done`, `blocked`.

---

## P0 — Blocking bugs

| # | Item | Status | Evidence |
|---|---|---|---|
| 1 | Google auto-login / session detection | done | New `frontend/src/services/googleOneTap.ts` (Google Identity Services One Tap, separate from the existing OAuth-redirect `signInWithGoogle`). Wired into `LandingView.tsx` (mount-time `tryGoogleOneTap`, only rendered while signed out) and `App.tsx`'s shared `endSession` (`suppressGoogleOneTapForSession()` on every sign-out, explicit or forced). `auto_select:true` makes GIS itself resolve silently only for a browser Google already recognizes as returning-and-consented; a never-seen account gets the visible, click-required One Tap bubble instead — this is what satisfies "don't auto-create silently" without a extra backend existence-check. All failure paths (script blocked, no session, exchange failure) resolve quietly, no toast. **Blocked on external config the user must supply**: a Google Cloud OAuth Web Client ID registered for this app's origin(s), entered as both `VITE_GOOGLE_CLIENT_ID` (see `.env.example`) and in Supabase Dashboard → Auth → Providers → Google → Client IDs. Code is inert (no-op) until that's set — `npx tsc --noEmit` clean either way. |
| 2 | Test entry: countdown + consent checkbox | done | Rewrote `LobbyView.tsx`: checkbox now gates a `consent` → `countdown` phase transition (countdown no longer auto-starts on mount). Unticked + click → inline `role="alert"` message + red-highlighted checkbox, no countdown. Ticked → countdown runs with no further button (`onStartTest` fires automatically at zero via a `hasNavigatedRef` guard, so a stray re-render/extra tick can't double-navigate). Consumption persisted per `testCode` in `sessionStorage` (`lumen_lobby_countdown_consumed_*`) so a remount after it already ran skips straight through. Added `frontend/src/pages/LobbyView.test.tsx` (4 cases: blocked-unticked, ticked-countdown-autonav, double-nav guard, persisted-skip) — all pass (`npx vitest run frontend/src/pages/LobbyView.test.tsx`). |
| 3 | Duplicate questions in a single test | done | Investigated live against the real DB (`DATABASE_URL` available this session) before changing anything: confirmed the frontend only ever creates tests through one path (`sessionApi.createSession` → `POST /assess/sessions` → `createPracticeTest` → `assembleForAttempt`), and a live query confirmed `content.question_node_map` currently has exactly 1 row per question (1400/1400) — so the query's join-fan-out risk (a question tagged to >1 syllabus node would make `assemble.ts`'s LATERAL yield 2 candidate rows for the same question_id, which could then both be picked) is real but was dormant against today's data, not the direct cause of any duplicate a user could have hit. Fixed it anyway (three layers, defense-in-depth, since the schema allows multi-node tagging even though today's importer never uses it): (1) `db/assess/test/generation/assemble.ts` — `group by q.question_id` in the candidate-pool LATERAL, `count(distinct question_id)` in the insufficient-pool availability check; (2) `db/assess/test/attempt/attempt-flow.ts` — a `seenQuestionIds` Set dedupes across both the BLUEPRINT and FIXED-paper code paths right before the bulk `attempt_question` insert (the one real choke point both sources funnel through — this is the actual "Fisher–Yates on a distinct ID pool" guarantee the item asks for, applied at the point of persistence rather than at render time), logging `console.warn` if it ever has to drop one; (3) `db/assess/test/definition/ingest-paper.ts`'s `ingestFixedPaper` (currently unreachable from any HTTP route — grepped `backend/`, zero callers — but exists for a future admin/content flow) now rejects a question repeated *across* sections of one paper, not just within one section (its existing unique constraint only ever covered the latter). The requested regression test already existed and already covers the exact assertion asked for: `db/assess/test/generation/assemble.test.ts`'s "no duplicate questions within one assembled paper" (`new Set(ids).size === ids.length`) — ran it live against the real DB before and after the fix, **6/6 pass both times** (`npx tsx --test db/assess/test/generation/assemble.test.ts`). `npx tsc --noEmit` clean. |
| 4 | Question images not rendering | done | Backend mapping was already correct and already shipped (an earlier session's Phase B3 work) — `envelope.ts`/`questionController.ts`/`getReview` all group `content.asset` rows by `question_id` correctly, no cross-question leakage. The actual gap was 100% frontend: `TestTakingView.tsx` and `AttemptReviewView.tsx` were rendering bare `<img>` tags with no reserved space (text/options reflowed when an image loaded) and no broken-image handling (a bad URL fell through to the browser's native broken-image icon). Added `frontend/src/components/ui/QuestionImage.tsx` — a shared slot with a fixed-height skeleton while loading, a `broken_image` fallback state on `onError`, and renders nothing when there's no url (so a no-image question still collapses to zero extra space, since callers already only mount it inside an `{image && ...}` conditional). Swapped it into both stem and option image spots in `TestTakingView.tsx` and `AttemptReviewView.tsx`. PDF export (`pdfExport.ts`) captures the live DOM via `html2canvas` with `useCORS: true` already set, so it inherits whatever the captured view renders — no separate fix needed there. `npx tsc --noEmit` clean. |
| 5 | Notifications module | done | Root cause: the backend already had real, working DB-backed endpoints for almost everything (`GET /learn/notifications`, `PATCH .../read-all`, generic `PATCH /:id` and `DELETE /:id` via `makeOwnedCrudRouter`) — the frontend `NotificationBell.tsx` just never called most of them. There was no per-item click-to-read (the service function `markNotificationRead` existed but was dead code, called from nowhere), and "Clear"/"Clear all" didn't exist anywhere, frontend or backend (no bulk-delete route existed at all). Added: `notification.repository.ts`'s `clearAll(userId)` (real `delete`, scoped to the caller) + `DELETE /learn/notifications` route (`learn.routes.ts`) for "Clear all"; `notificationsApi.ts`'s `clearNotification`/`clearAllNotifications` (the single-item `DELETE /:id` route already existed server-side, just had no frontend caller either). Rewrote `NotificationBell.tsx`: clicking an unread notification marks it read; a per-row "Clear" (×) button removes it; a "Clear all" action added next to "Mark all read". Every mutation (including the pre-existing "Mark all read", which previously had none) is now optimistic-with-rollback — local state updates immediately, reverts to the pre-action snapshot if the server call rejects. Unread badge count is derived from `notifications` state, so it recalculates automatically with every one of these. Read/cleared state was already correctly scoped to `user_id` in the DB (survives logout/login/device change by construction — nothing client-side to persist). Added `frontend/src/components/ui/NotificationBell.test.tsx` (5 cases: mark-one-read + badge update, rollback-on-failure for mark-one, clear-one removes from list, clear-all empties the list, rollback-on-failure for mark-all) — all pass (`npx vitest run frontend/src/components/ui/NotificationBell.test.tsx`). `npx tsc --noEmit` clean. |

## P1 — Missing features

| # | Item | Status | Evidence |
|---|---|---|---|
| 6 | Demo account login | done | Most of this already existed (`demoSession.ts`'s `ensureDemoSession`, a "Demo Account" button on the login form) but was incomplete/buggy. Added: "Try Demo Account" button on the **register** form too (`LandingView.tsx`, was login-only). "Demo Mode" badge in `Header.tsx` (compares `profile?.email` against a newly-exported `DEMO_EMAIL`/`isDemoEmail()` from `demoSession.ts` — zero new prop-drilling since Header already fetches its own `MeProfile`). Found and fixed a real latent bug while building the seed script: this Supabase project has "Confirm email" enabled, so `ensureDemoSession`'s `signUp()` call could succeed with no error but also no session (account created but stuck unconfirmed) — the old code treated that as success and let `handleDemoAccountLogin` call `onLoginSuccess` with no real session behind it, reintroducing the exact 401-bounce-back bug that same call site's own comment says was already fixed once; now throws a clear error in that case instead. Seeded realistic sample data: new `db/scripts/demo/seed-demo-account.ts` (idempotent, targets 5 scored attempts, ~72% simulated accuracy with ~8% realistic skip rate, timestamps spread across the last 2 weeks for a believable trend) — **ran it live this session** (user-approved): provisioned the demo account via the admin API (pre-confirmed, since a stuck-unconfirmed row from an earlier manual click already existed under this email) and seeded 5 real scored attempts (PHY 36/60, CHEM 25/60, BOT 45/60, ZOO 41/60, PHY 28/60) dated Aug 16–26 2026. Verified idempotent: re-ran immediately after, correctly reported "already has 5 scored attempts — nothing to seed." Scheduled reset: `.github/workflows/reset-demo-account.yml` (weekly cron + manual dispatch, `--reset` flag wipes and reseeds) — **needs a `SUPABASE_PUBLISHABLE_KEY` repository secret added** (ci.yml only had `SUPABASE_SERVICE_ROLE_KEY`) before its schedule will actually run; flagged for the user. "All features unlocked": no separate premium/paywall gate exists anywhere else in the app for the demo account to bypass (confirmed by grep — the only lock found anywhere was item 9's, already removed). `npx tsc --noEmit` clean. |
| 7 | IRT analysis report | done | Built a real, if simplified, IRT model — not fabricated numbers. `db/assess/analytics/irt-model.ts`: pure-math Rasch (1-parameter logistic) Joint Maximum Likelihood Estimation (JMLE, Wright & Panchapakesan 1969) — alternating Newton-Raphson updates for every item's difficulty holding ability fixed, then every person's ability holding difficulty fixed, re-centering each round (Rasch is only identified up to an additive constant); a continuity correction handles perfect/zero raw scores without diverging. 8 unit tests (monotonic ordering, mean-anchoring, standard-error-shrinks-with-more-data) — all pass, verifying the math itself independent of any DB. `db/assess/analytics/irt.ts`: DB orchestration — calibrates item difficulty from every scored attempt's responses platform-wide (Rasch calibration needs the whole population, not one test), then estimates θ for one specific attempt against those fixed difficulties, plus an ability trend across the user's own attempts reusing the same calibration. **Found live, testing against this session's real seeded demo data, that raw item/person-count thresholds are the wrong gate**: with only 5 scored attempts and little item overlap between them, JMLE technically "converges" but to wildly unstable estimates (θ≈3 with standard error≈440) — added a standard-error ceiling (`MAX_TRUSTED_STANDARD_ERROR = 2.0`) as the real "not enough data" signal instead, which correctly downgrades every one of those 5 real attempts to the explicit not-enough-data state rather than showing a misleadingly precise-looking number. `GET /api/assess/attempts/:id/irt` (new controller/route, mirrors the existing `/review` endpoint's ownership/auth pattern exactly). Along the way found and fixed a real pre-existing gap: `ReviewNotAvailableError` (thrown by the existing `/review` endpoint too, not just this new one) was never mapped in `errorHandler.ts`, silently falling through to a generic 500 instead of a real 409 — added it to the status table. Frontend: `frontend/src/components/ui/IrtSection.tsx` — θ/SE/plain-language band cards, an "Ability Trend Across Attempts" line chart, an "Ability vs. Item Difficulty" scatter (both `recharts`, already a project dependency), an info-icon "What is IRT?" modal explaining θ/standard error/how it differs from a raw percentage in plain English (no jargon), and its own explicit "not enough data yet" empty state showing the server's real reason — never a placeholder number. Wired into `AttemptReviewView.tsx` (also serves item 10/11's "detailed report" destination). `npx tsc --noEmit` clean; widened `test:unit`'s glob to include `db/assess/analytics/*.test.ts` (was silently excluded before). |
| 8 | Practice test → unit flow | done | `DashboardView.tsx`'s "Where You're Lagging" section (`analytics.weakestUnits`, real per-unit accuracy, already shown post-test since `handleEvaluationComplete` in `App.tsx` drops the user straight onto the dashboard tab) already displayed unit name + accuracy per card, but the only CTA was one bulk "PRACTICE THESE UNITS" button that called `onTakeTest` — a generic hand-off to the test directory tab, not a link into any specific unit, exactly the bug the item names. Added a `catalogTree`/`onSessionCreated` prop pair to `DashboardView` (App.tsx already had both, already passed to `TestListView`/`CourseAreaView` the same way) and `handleStartUnitTest(unit)`: resolves the unit's real `subjectId` from `catalogTree` (analytics only carries `subjectCode`), calls `createSession({ mode: "subject-wise", subjectId, syllabusNodeId: unit.nodeId, ... })` — same real pattern `CoursesView.tsx`'s `handleStartUnitMock` already used elsewhere — and hands the resulting session to `onSessionCreated`, landing the student directly in that unit's test. Each weakest-unit card now has its own "Start Unit Test" button (per-card loading state, `POOL_INSUFFICIENT`-aware error banner); removed the old bulk button since every card is now its own direct link. Side finding while in there: the empty/first-time hero state ("Take your first mock test...") had text but no button at all — `onTakeTest` was passed into `DashboardView` but never actually called anywhere; added a real "Take Your First Test" CTA there too. `npx tsc --noEmit` clean. |
| 9 | Unlock full mock test | done | `TestListView.tsx`'s Full Mock card was gated behind `isSyllabusCompleted` (real logic — App.tsx's `chapterGoals` checklist, not a stub). Added `frontend/src/config/featureFlags.ts`'s `FULL_MOCK_REQUIRES_SYLLABUS_COMPLETION = false`; `TestListView.tsx` now derives `fullMockUnlocked = !FULL_MOCK_REQUIRES_SYLLABUS_COMPLETION \|\| isSyllabusCompleted`, so with the flag off (default) the card, button, and copy are always the unlocked state — no lock icon/"Locked (Complete Syllabus Tracker)" copy renders. Flip the flag back to `true` to restore the gate with no other code changes. `npx tsc --noEmit` clean. |
| 10 | Detailed report + overall user analysis | done | `AttemptReviewView.tsx` already covered question-level review (user's answer/correct answer/explanation/image/time spent — an earlier phase's work) and now also carries items 7's IRT section and 12's branded PDF download; this item filled in everything else the spec asks for that was still missing. Overall score/accuracy/attempted-vs-unattempted/time-taken-vs-allotted and a named section-wise breakdown: extended `getScorecardWithSections` (`attempt-flow.ts`) — it previously returned bare `section_score` rows with no human-readable name and no attempt/test timing at all — to join `assess.test_section` for `section_name`/`question_count` and add a `timing` block (`started_at`/`submitted_at`/allotted `duration_minutes`); exposed via the existing `GET .../scorecard` route (just enriched the response, same endpoint). Comparison against the cohort average: new `getCohortComparison` (`dashboard.ts`) — **found live that "cohort" can't mean "everyone who took this exact test"**, since `createPracticeTest` mints a brand-new `assess.test` row every single session (confirmed in Phase C's own history), so almost no two attempts ever literally share a `test_id`; redefined cohort as every other scored attempt of the same *shape* (same TEST_TYPE+SCOPE_CODE segment of `test_code` — e.g. every PHY subject-wise practice, reusing the exact prefix convention `test-code.ts` already defines) — real, verified live against the seeded demo data (correctly found the other PHY attempt, averaged its 66.67% accuracy, excluding the current attempt from its own comparison). New `GET /assess/attempts/:id/cohort` route. Progress over time: reused the existing `useDashboardAnalytics` hook's `scoreTrend` (no new backend) inside the new `frontend/src/components/ui/ReportSummary.tsx`, which renders all of the above — overview cards, a section table with a computed Strength/Developing/Needs Improvement tag per section, a cohort-average bar comparison, and a trend line marking the current attempt. Downloadable as PDF: already covered by item 12's work on this same view. Verified live against real seeded data (script run, output inspected, then deleted) before wiring the UI. `npx tsc --noEmit` clean; `npx vitest run` 34/34. |
| 11 | "View results" in profile menu | done | Found a "View Results" menu item already existed in the profile dropdown (`Header.tsx`) — but it just called `handleNavigation("analytics")`, routing to the existing chart-only `AnalyticsView` (confirmed via grep: no attempt-list table exists anywhere in that view or anywhere else in the app). Real gap, not a stub. Backend: `listAttempts` (`attempt-flow.ts`) only returned `attemptState`/marks/dates, not the type filter the item asks for — `assess.test.test_mode` turned out to never actually be populated (`createPracticeTest` never passes it through), so extended `AttemptSummary` with `mode`/`testCode`/`durationMinutes`, deriving `mode` from `test_code`'s own embedded TEST_TYPE segment via the already-existing `deriveSessionModeFromTestCode` (same derivation `dashboard.ts`/`envelope.ts` already use elsewhere — no new persistence, no duplicated logic). Added `listMyAttempts()` to `sessionApi.ts` (same `GET /assess/attempts` `getActiveSession` already calls, just returning the full list instead of filtering to one resumable attempt). Built `frontend/src/pages/MyResultsView.tsx`: every attempt, sortable (test name/date/score, click column header) and filterable (type, date range), paginated (10/page), status-badged (in_progress/paused/submitted/scored/abandoned), each row's "View" opens the detailed report (`AttemptReviewView` — the same component item 7's IRT section and item 10's fuller report live in) and "Download" triggers a PDF export of that same report. Wired as a real new tab: `App.tsx` (`pathToTab`/`MyResultsView` lazy import/render block) and `Header.tsx` (`tabToPath`, and the "View Results" button now actually calls `handleNavigation("results")`). `npx tsc --noEmit` clean; full `npx vitest run` still 34/34. |
| 12 | Report PDF branding | done | Confirmed there's no separate "question-paper" PDF generator anywhere in the repo to match against (grepped for `jsPDF`/`pdfmake`/`puppeteer` — only `frontend/src/services/pdfExport.ts`'s `html2canvas`-DOM-capture approach exists) — every "report PDF" is a screenshot of whatever's on screen, so branding it means branding the captured screen, not a separate PDF template. Both existing captured containers (`#analytics-report-container` in `AnalyticsView.tsx`, and the review view now captured as `#attempt-report-content`) had **zero** LUMEN ACADEMY branding — a plain unbranded screenshot. Added `frontend/src/components/ui/ReportBrandHeader.tsx` — logo (`LumenLogo`), "LUMEN ACADEMY" heading, and the app's actual real tagline copied verbatim from `Header.tsx`/`App.tsx`'s own footer ("Empowering Future through Learning" — the item's own phrasing said "Futures", but that's not this app's real established tagline anywhere else, so matched the app instead of the prompt's paraphrase), teal/navy-with-gold-accent theme matching the rest of the app. Wired into both containers. While in there, gave `AttemptReviewView.tsx` its own self-contained "Download Report (PDF)" button (previously it had no download capability at all — item 11's list-row download button now just triggers this same one instead of a bespoke re-implementation), so downloading works from every entry point (Dashboard's "View Detailed Report", the results list, standalone). `npx tsc --noEmit` clean; `npx vitest run` still 34/34. |

## P2 — Performance & polish

| # | Item | Status | Evidence |
|---|---|---|---|
| 13 | Lag and loading performance | done | **Evaluation animation** (the item's clearest, most concrete complaint): confirmed via `App.tsx`'s `handleSessionComplete` that the real scoring already finishes (server `submitAttempt`) *before* `EvaluatingView` ever mounts — nothing is actually being evaluated on this screen, it was purely decorative. Its old random-increment simulation (`Math.random()*6+2` per 150ms tick) could take anywhere from ~2s to 7s+ by chance, then bolted on a flat extra 2000ms regardless — exactly "blocking with no fixed end." Rewrote it as a deterministic, elapsed-time-based animation capped at the item's own 2s ceiling (`ANIMATION_MS = 1500`), transitioning the instant it completes — never longer, never a loop. Also removed a hardcoded fake status line ("Preparing report for NEET Biology Mini-Mock #12") that named a specific test regardless of what was actually taken — now shows the real attempt title. **Profile**: `fetchMe()` (`meApi.ts`) only ever deduplicated truly-concurrent calls, not repeat calls over time — every Dashboard/Profile/Header mount re-fetched from the network. Added a 60s TTL cache (`cachedMe`), invalidated immediately on `updateMe()` (so a save is never stale) and explicitly on sign-out (`clearMeCache()`, wired into `App.tsx`'s `endSession`) — the invalidation-on-sign-out matters for correctness, not just performance: without it, a fast account switch (e.g. into the demo account) could briefly show the *previous* account's cached profile. `ProfileCard.tsx` already had a real skeleton (`animate-pulse` placeholder), not a blank screen — confirmed, not fabricated. Attempt-history pagination: no over-fetching page existed under "Profile" specifically (`ProfileCard.tsx` doesn't list attempts at all); the closest real match is item 11's new results list, which already fetches a small, real dataset and paginates client-side — reasonable at this app's actual data volume (single digits to low hundreds of attempts per user), so left as-is rather than building server-side pagination no current data justifies. **N+1 audit**: found and fixed one real N+1 — in *this session's own* `irt.ts` ability-trend loop, which re-queried per historical attempt; refactored to group the already-fetched population-wide response set once instead (see item 7's tracker entry). Audited the rest of the results/report path (`getScorecardWithSections`, `getCohortComparison`, `listAttempts`) — each already a single query or a small fixed number of queries, no loop-per-row pattern found. **Indexes**: reviewed `db/migrations/023_analytics_indexes.sql` (an existing, well-targeted index migration for exactly this query family) plus the new queries added this session (`getCohortComparison`'s `test_code like` prefix match rides the existing unique index on `test_code`; the `getScorecardWithSections` join to `test_section` is PK-indexed) — no missing index found worth adding at this app's current data scale. **Images**: added `loading="lazy"` + `decoding="async"` to `QuestionImage.tsx` (a real, safe, immediate win — the item's larger ask, server-side compression/resizing of Supabase Storage originals, needs infra/plan-tier decisions this session can't make safely, so left as an explicitly flagged gap, not attempted blind). **Re-renders**: no obvious unmemoized-heavy-component hotspot found beyond what's already `useMemo`'d (`TestTakingView.tsx`'s `orderQuestions`, `MyResultsView.tsx`'s filter/sort pipeline). **Code-splitting**: `AttemptReviewView.tsx` was imported eagerly by both `DashboardView.tsx` and `MyResultsView.tsx` — since this session's own work (items 7/10/12) added `recharts` + `jsPDF`/`html2canvas` to it, that meant every user's very first post-login chunk (Dashboard) now carried that weight even if they never opened a report. Converted both call sites to `React.lazy` + `Suspense`. **Timing logs**: added `backend/src/middleware/requestTiming.ts` — one line per request (method/path/status/elapsed ms), anything ≥500ms prefixed `SLOW` and grep-able; wired into `server.ts`. Verified live: restarted the dev server, hit all four new/changed endpoints (`/irt`, `/cohort`, `/scorecard`, `/attempts`) through the real Express app with a real demo-account token — all 200, correct payloads. `npx tsc --noEmit` clean throughout. |
| 14 | Dashboard scorecard text fixes | done | Added `frontend/src/utils/pluralize.ts` (`pluralize`/`countLabel`) as the one shared rule, then swept for real instances: `DashboardView.tsx`'s STUDY STREAK tile had both bugs the item names — `<AnimatedCounter value={studyStreak} /><span>...Days</span>` with no space between them (`ml-1` on the wrong element did nothing useful; switched the wrapper to `flex items-baseline gap-1.5`) and "Days" hardcoded regardless of count (now `pluralize(studyStreak, "Day", "Days")`, so a 1-day streak reads "1 Day" not "1 Days"). Same fix in `Header.tsx`'s streak badge tooltip (`"${studyStreak} Day Study Streak"` → pluralized). `TestListView.tsx`'s custom-test title suffix (`` `[${selectedList.length} units]` ``, unconditionally plural) now uses `countLabel`. Audited the rest of `DashboardView.tsx` for the same two failure modes (missing space, wrong plural) — everything else either already had a natural space (e.g. "You scored 3 marks") or is a ratio label ("3 / 45 questions", "12✓ / 3✗ / 1 skipped") where the noun describes the pairing, not a single count, so pluralizing on the denominator would read worse, not better; left those alone rather than over-applying the rule. Added `frontend/src/utils/pluralize.test.ts` (4 cases: singular at 1, plural at 0 and >1, default plural suffix, guaranteed spacing) — all pass. `npx tsc --noEmit` clean. |
| 15 | Motivational scorecard adjectives | done | Found the real static label the item describes: `DashboardView.tsx`'s hero heading was a hardcoded `Great work, {studentName}!` for every real attempt — a 30% and a 95% accuracy got the identical headline; no score-band or scenario logic existed anywhere in the frontend (grepped for it first). Added `frontend/src/utils/motivationalMessage.ts`: priority-ordered scenarios (first attempt → real improvement-since-last-attempt, using `analytics.attemptHistory`, which is most-recent-first so `[1]` is the genuinely-previous attempt → consistency streak (`studyStreak >= 3`) → accuracy band high/mid/low), each band a 3-phrase pool rotated by a `variationSeed` (`attemptsCount`, so it varies attempt-to-attempt without flickering mid-render). Hard rule enforced by construction, not just by convention: no phrase pool anywhere contains discouraging wording — the low band is "Great start", "building momentum", "here's where to focus next", never "Poor/Weak/Bad/Failed". Every message pairs with a concrete next step (`analytics.weakestUnits[0].unitTitle` when available, else a generic "take another test"). Wired into `DashboardView.tsx`'s hero (replacing the static headline, and adding the next-step line beneath the existing "You scored X marks..." sentence). Added `frontend/src/utils/motivationalMessage.test.ts` (7 cases: no negative wording across the full 0–100 accuracy range, next-step always present incl. the no-weakest-unit-yet fallback, first-attempt greeting, real-improvement detection vs. flat/dropped accuracy, band rotation actually varies) — all pass. `npx tsc --noEmit` clean. |

---

## Session log

### Session 1 (2026-08-29)
- Read `docs/assessment-tool-fix-prompt.md` in full. Confirmed no prior tracker existed for it (file added in commit `9e16c24`, this session is the first to act on it).
- Created this tracker.
- Worked P0 → P1 → P2 in order across two sessions (context reset partway through; resumed cleanly from this file per its own instructions at the top). **All 15 items done.**
- Final verification before closing out: `npx tsc --noEmit` clean; full `npm run test:unit` (backend + db, live DB) **62/62 pass**; `npx vitest run` (frontend) **34/34 pass**; `npm run build` succeeds — confirmed via the chunk output that item 13's code-splitting actually took effect (`DashboardView` chunk dropped to 61kB with `recharts`/`jsPDF`/`html2canvas` isolated into their own on-demand chunks, e.g. `pdfExport-*.js` at 594kB loads only when a report/PDF is actually opened). Also smoke-tested all four new/changed attempt-report HTTP endpoints (`/irt`, `/cohort`, `/scorecard`, `/attempts`) live through the real Express app with a real demo-account bearer token — all 200 with correct payloads.
- Live-DB mutations made this session (both user-approved before running): (1) item 6's `db/scripts/demo/seed-demo-account.ts` — provisioned the demo account (pre-confirmed via the admin API, since a stuck-unconfirmed row already existed from an earlier manual click) and seeded 5 real scored attempts; re-run afterward and confirmed idempotent. (2) Nothing else touched live data — every other fix is application code.
- Two items remain genuinely blocked on things outside this session's control, flagged clearly in their own rows above rather than silently left incomplete: **item 1** (Google One Tap) needs a real Google Cloud OAuth Client ID the user must register and enter as `VITE_GOOGLE_CLIENT_ID` + in Supabase's Google provider settings; **item 6**'s scheduled reset workflow needs a `SUPABASE_PUBLISHABLE_KEY` repository secret added (only `SUPABASE_SERVICE_ROLE_KEY` existed before). Everything else is fully working code, verified live where the finding claims "verified live."
- Real bugs found and fixed along the way that weren't explicitly one of the 15 items, each noted in its relevant row above: `demoSession.ts`'s silent-success-with-no-session bug (email confirmation edge case), `errorHandler.ts` never mapping `ReviewNotAvailableError` (fell through to a generic 500), the Dashboard hero having no CTA at all for a brand-new user, and the N+1 query this session's own IRT work introduced (caught and fixed before shipping).
