# TEST_LAYER_HARDENING_TRACKER — running tracker for docs/test-layer-hardening-prompt.md

Source directive: `docs/test-layer-hardening-prompt.md`. Scope: test layer only (test generation service, attempt lifecycle, session lockdown, blueprint/weightage, question pool health, scoring). Fixed order: **Audit → Derive bugs → Derive test cases → Fix → Verify.** No fixing before the audit (`AUDIT.md`) and bug list (`BUGS.md`) are written down.

This file is the single source of truth for progress on this directive. Update status/evidence as work lands; do not rewrite history.

**Relationship to other active directives in this repo:** `docs/assessment-tool-debug-plan.md` (tracked in `docs/ASSESSMENT_DEBUG_TRACKER.md`) is a broader whole-app live-QA pass, already done through P0-P5/P7-P10 (P6 blocked on external Google Drive sharing, DB-05 i18n sweep deferred). That pass touched the test/attempt lifecycle at a UX/bug-report level (ghost tests, resume, cross-user bleed, repeated questions, wrong options, Tamil completeness) and fixed several real issues there (see its tracker Session 1 Phase 2/4). **This directive goes deeper into the same subsystem** — DB-level uniqueness constraints, concurrent-generation races, blueprint-as-data, image test type as first-class, duplicate-at-source detection, and a much larger test-case matrix. Treat prior fixes as a starting point to verify against, not as proof an area is already hardened — re-check with fresh evidence per this directive's own audit questions rather than assuming the earlier pass covered it.

**If the user says "continue" (or similar, with no other context) and this directive is the most recently active one:** read this file's Session log (bottom) first, then resume at the exact phase/step recorded, re-verifying anything marked "in progress" against live state before trusting it. Per the standing autonomous-operation rule already established in this repo (`docs/ASSESSMENT_DEBUG_TRACKER.md`'s header): work through phases without pausing for confirmation, except before genuinely destructive/irreversible actions against shared state (running a real dedup/retirement against the live DB, force-pushing, dropping tables) — those get an explicit call-out. Stop at a clean phase boundary and tell the user to start a new session when token budget runs low.

Status values: `todo`, `in-progress`, `done`, `blocked`.

---

## Phase status

| Phase | Deliverable | Status | Notes |
|---|---|---|---|
| 0 | `docs/AUDIT.md` (read-only) | done | Committed, §1.1-1.6 all answered with file:line citations |
| 1 | `docs/BUGS.md` | done | 40+ bugs/non-repros across A-F plus new G1 (CRUD-bypass) |
| 2 | `docs/TEST-CASES.md` | done | 170 cases across 8 buckets, all floors met |
| 3 | Fixes (A repetition → B/C session → D blueprint → E image type → F config) | in-progress | A (all), B (all), C1/C3/C4 done; remaining: C9 (needs product clarification), D, E, F |
| — | R1 pool health matrix | todo | |
| — | R2 duplicate report | todo | needs schema-name correction against real `content.question`/`content.question_option` before running (prompt's own SQL is best-guess) |
| — | R3 blueprint fit simulation (100 seeded generations) | todo | depends on D1 (blueprint-as-data) fix landing first for it to be meaningful |
| — | Definition of done checklist (§6 of the prompt) | todo | |

---

## Session log

### Session 1 (2026-08-31)
- Read `docs/test-layer-hardening-prompt.md` in full. Confirmed no prior tracker existed for this directive; created this one.
- Checked for overlap with the existing `docs/assessment-tool-debug-plan.md` pass (already done through most phases) — related but this directive is a deeper, narrower pass on the same subsystem; documented the relationship above rather than assuming duplicate work.
- **Phase 0: done.** Three parallel read-only research passes (repetition/pool, session/attempt lifecycle, blueprint/image/test-type) each produced file:line-cited findings; synthesized into `docs/AUDIT.md` following the prompt's exact §1.1-1.6 structure. Key findings carried forward: no `UNIQUE(test_id,question_id)` constraint (only section-scoped); `assembleForAttempt` doesn't participate in `startAttempt`'s transaction and will deadlock the 4-connection pool under concurrent load (same bug pattern as an already-fixed `loadSectionSchemes` case, fix not applied here); anti-repeat window described in `candidate-pool.sql` (50-attempt hard exclusion) doesn't match the live `assemble.ts` code (no window at all, soft-sort only); a generic `PATCH/DELETE /api/assess/attempts/:id` CRUD route bypasses the entire attempt state machine including the server-authoritative deadline; resume does not restore answers/flags/index/time despite the server correctly persisting all of them; no session-lockdown mechanism exists anywhere (client or server) beyond the exam timer; `catalog.node_weightage` is a fully-built, wired table that `assemble.ts` never reads, while a second unsynchronized hardcoded weightage array lives in the frontend; `has_image` is manually set and confirmed (via the codebase's own diagnostic scripts) to already drift from reality; test-type taxonomy is encoded independently in 3 places with no single source of truth.
- **Phase 1: done.** Built `docs/BUGS.md` directly from the audit's citations — went through every lead in the prompt's §2 (A1-F4) and confirmed, refuted, or marked N/A each with evidence rather than assuming the lead list was the final answer; found and added one new bug not in the prompt's lead list, **G1** (the CRUD-bypass finding), flagged as arguably the single most severe issue in the whole audit since it undermines the "server-authoritative" claims for both timer (C2) and state machine (B/C) simultaneously. Also flagged one genuine open policy question rather than guessing: whether the current "never hard-exclude a seen question" (D-2) design should stay as-is or be replaced with a hard anti-repeat window — the prompt's own boundary-case list assumes a hard window exists to fail loudly against, but audited behavior has no window at all.
- **Phase 2: done.** Built `docs/TEST-CASES.md`, 170 cases across the 8 required buckets (all floors met/exceeded), each linked back to a `BUGS.md` ID where applicable, with automation status assessed against the real existing test infra (`node:test`/Vitest under `db/`, Playwright under `tests/`). Flagged REP-13 as not cleanly executable until the D-2 policy question above is resolved, and RES-22/C9 as blocked on confirming whether a scheduled-test-window feature exists at all (no `due_date`/`window_start`/`window_end` columns found on `test_assignment` in this pass).
- **Phase 3: started, first batch landed (A4, A4b, G1).** All three verified live against the real dev DB, not just read/reasoned about:
  - **A4 (pool-exhaustion deadlock):** `assembleForAttempt` (`db/assess/test/generation/assemble.ts`) now takes an optional `client` parameter (defaults to `pool`, same pattern as the already-fixed `loadSectionSchemes` in the same file) and `startAttempt` (`attempt-flow.ts:215`) threads its own transaction client through instead of letting it fall back to the shared 4-connection pool.
  - **A4b (TOCTOU race on one-active-attempt-per-user):** the active-attempt check in `startAttempt` moved from a plain pre-transaction query to inside the transaction, guarded by `pg_advisory_xact_lock(hashtext(user_id::text))` taken first — a second concurrent caller for the same user now blocks on the lock instead of racing past a stale read.
  - **G1 (generic CRUD bypass of the attempt state machine):** `backend/src/routes/assess.routes.ts` no longer mounts `assess.attempt` through `makeOwnedCrudRouter` (which exposed unrestricted `PATCH`/`DELETE /:id`, letting an attempt's own owner overwrite `attempt_state`/`server_deadline`/`paused_ms_total` directly or delete the row outright). Replaced with a hand-built router: `requireAuth` + a read-only, ownership-checked `GET /:id` (confirmed via grep that the frontend never called the generic PATCH/DELETE/bare-GET routes — only the purpose-built sub-routes), then all the existing purpose-built sub-routes (start/responses/submit/pause/resume/envelope/review/scorecard/irt/cohort) mounted exactly as before.
  - **New regression test**, `db/assess/test/attempt/concurrent-generation.test.ts`: fires 4 concurrent BLUEPRINT-mode `startAttempt` calls (== `pool.max`) across 4 different users and asserts all complete within a bounded time instead of hanging (proves A4); fires 2 concurrent `startAttempt` calls for the *same* user and asserts exactly 1 succeeds with the other rejecting `ActiveAttemptExistsError`, then confirms via a direct DB count that only 1 active attempt row exists afterward (proves A4b). **Ran live against the dev DB, both sub-tests pass** (not just "would pass in theory").
  - **Verified**: `npx tsc --noEmit` clean; `npm run test:unit` 69/69 node:test (now including the 3 new assertions in the concurrent-generation file, run separately and confirmed passing) + 34/34 Vitest, all still green; `npm run build` clean (frontend + backend bundle).
  - **Did not attempt to reproduce the pre-fix hang live** (e.g. by temporarily reverting and re-running) — deliberately avoided, since the whole point of the bug is an indefinite hang that could tie up a live shared dev database's connections; confidence instead comes from the fix being the exact same shape as the already-proven `loadSectionSchemes` fix in the same file, plus the new test passing cleanly post-fix.
- **Not yet started:** A1 (the `UNIQUE(test_id, question_id)` migration), A6/A10 (exposure-ledger timing, seed reuse), all of B/C (session lockdown, resume restoration, expiry sweeper — large frontend + backend work), D (blueprint-as-data — needs a migration plus a real cited NTA bulletin source, which this session does not have and should not fabricate), E (image test type — needs the group concept built for the first time), F (test-type config consolidation). None of the three reports (R1/R2/R3) started.
- **Flagging for the user rather than guessing further:** (1) A1 needs a schema migration (most likely denormalizing `test_id` onto `assess.test_question` or a constraint trigger against `test_section.test_id`) — per this repo's own established convention (a schema change gets proposed before applied), want to confirm the denormalization approach before writing the migration. (2) D1's versioned blueprint work explicitly requires sourcing real unit weightage from "the official NTA information bulletin for the target year," which needs an actual document/citation from the user, not the model's memory or a coaching-site number — cannot proceed on that specific sub-task without it. (3) B (session lockdown) is a genuinely large, multi-file frontend build (route guards, `beforeunload`, fullscreen, tab-integrity logging, an API-lockdown allowlist) — next natural chunk of work, no blocker, just sizeable.
- User said to continue autonomously: propose+apply the A1 migration, skip D1's NTA-sourced numbers for now (flagged clearly, not fabricated), keep working the rest in priority order.
- **A1 landed and verified live.** `db/migrations/027_test_question_cross_section_unique.sql` + `db/verify/verify_027_test_question_cross_section_unique.sql`: denormalized `test_id` onto `assess.test_question` (backfilled, kept in sync by a `before insert or update of test_section_id` trigger), backed by `unique (test_id, question_id)`. **Checked before applying**: a live query confirmed zero pre-existing cross-section duplicates (would have blocked the migration — the correct failure mode, not worked around). **Verified after applying**: ran the migration + verify script live via `node db/scripts/run-migration.mjs`, then proved the constraint actually rejects a real cross-section duplicate insert via a rolled-back transaction against live data (no real data touched). `TestQuestionModel` updated with the new column.
- **A6 landed and verified live.** `startAttempt` now upserts `assess.user_question_seen` for every served question at serve time (not just at submit), so an abandoned attempt's questions are no longer treated as fully unseen on the next generation; `submitAttempt`'s own write no longer double-increments `times_seen` for the same exposure. New test `db/assess/test/attempt/anti-repeat-exposure.test.ts`, both assertions passing live.
- **A10 landed, with a real cross-fix interaction found and documented, not hidden.** Added `reproduceAttemptAssembly` (re-runs `assembleForAttempt` with an attempt's stored `generation_seed`). **While writing its test, found live that A6's own fix breaks A10's original premise**: marking a question "seen" at serve time changes the very sort key the candidate query uses, so reproducing a draw *after* the attempt that served it now generally returns a different question set — confirmed with a real side-by-side diff, not assumed. Rewrote the test and the function's own docstring to state plainly what's actually still guaranteed (seed+state determinism, proven by two back-to-back calls with no attempt started in between) versus what isn't (a byte-for-byte replay once A6 has touched the exposure ledger). Logged as a documented interaction in `BUGS.md#A10`, not swept under the rug.
- **Real test-infrastructure bug found and fixed along the way, not anticipated going in**: adding 3 new DB-heavy integration test files onto a shared dev database with only 9 `core.app_user` rows caused genuine cross-file races — one file's own "clear stale attempt state for this user" setup query would abandon a *different* concurrently-running file's live, legitimate attempt for that same user (confirmed live via `InvalidStateTransitionError: cannot transition from 'abandoned' to 'submitted'`), not just a transient retryable collision. Fixed properly, not papered over: all 5 attempt-lifecycle integration test files (`assemble.test.ts`, `cross-user-isolation.test.ts`, `concurrent-generation.test.ts`, `anti-repeat-exposure.test.ts`, `reproduce-assembly.test.ts`) now select their `core.app_user` row(s) from an explicit, disjoint, ascending-`user_id` partition of the same 9-row table (rows 1-2, 3, 4, 5, 6-9 respectively — documented cross-referentially in each file), so true overlap is no longer possible rather than just less likely. Kept a retry-on-`ActiveAttemptExistsError` wrapper in each file too, as defense-in-depth for environments where the table has a different row count than this dev DB's current 9. **Verified stable across 2 consecutive full runs** (78/78 node:test passing both times, no flakes), not just a single lucky green run.
- **Verified (this whole batch — A1, A6, A10, plus the test-infra fix)**: `npx tsc --noEmit` clean; `npm run test:unit` 78/78 node:test (up from 69 at the start of this session) + 34/34 Vitest, run twice for stability; `npm run build` clean (frontend + backend bundle).
- A-priority (repetition/concurrency) is now fully done per `BUGS.md` (A1, A4, A4b, A6, A10 fixed and verified; A2/A5/A8 confirmed not reproduced; A3/E6 forward-looking, deferred to the image-type build; A7/A9 are data-task/deferred-feature items, not code fixes for this pass).
- **C4 landed and verified.** `EnvelopeResponse` gained `timeSpentSeconds` (`envelope.ts`); `TestTakingView.tsx`'s answers/flags/time-map/current-index are now all seeded from `session.responses` on mount and on attempt-id change, going with "derive current index from first unanswered question" rather than a new persisted column (no migration needed). New Vitest cases in `TestTakingView.test.tsx`, both passing, stable across repeated runs.
- **C3 landed and verified.** Extracted the sweep query into a shared `sweepExpiredAttempts()` (`db/assess/test/attempt/expiry.ts`); new `backend/src/jobs/expirySweeper.ts` runs it on a 60s `setInterval` inside the live server process, wired into `server.ts`'s boot/shutdown; `db/scripts/sweep-expired-attempts.ts` is now a thin wrapper over the same function instead of duplicating the query. New integration test `expiry-sweeper.test.ts` simulates the literal Definition-of-Done scenario (deadline passes, nothing ever reconnects, only the sweeper touches it) and passes live.
- **A real, live test-infrastructure incident, handled transparently rather than papered over:** while adding the C3 test (a 6th DB-heavy integration test file sharing this dev DB's 9-row `core.app_user` table with the 5 files added earlier this session), the *previous* mitigation attempts were found to be actively unsafe, not just imperfect:
  1. A static disjoint-offset partition across files (documented in each file) — worked until a 6th file needed a slot none of the 9 rows had left, and was fragile to begin with (breaks the moment the table's row count changes or a 7th file is added).
  2. A Postgres session-level advisory lock (`pg_advisory_lock`) serializing every file's critical section — correct in principle, but discovered live to be genuinely dangerous in practice: when `concurrent-generation.test.ts`'s own hardcoded 60s test-timeout fired *while it held the lock* (because it was now also queueing behind other files for the same lock, eating into that same 60s budget), node's test runner marked the test failed but did **not** cancel the still-in-flight async work — the lock was never released, and the entire rest of the test run hung indefinitely waiting for a lock nothing would ever free. Confirmed live (had to `TaskStop` a genuinely stuck background process), then confirmed the kill cleanly released the DB-side advisory lock (checked `pg_locks`/`pg_stat_activity` directly — zero lingering locks or idle-in-transaction sessions) before touching anything else.
  - **Actual fix**: removed the custom lock entirely (deleted `_shared-integration-test-lock.ts`) and the retry-on-collision wrappers from all 6 files, and instead added `--test-concurrency=1` to `package.json`'s `test:unit` script — node's own test runner serializes every matched file, with no custom coordination code (and thus no way for one file's bug to leave a lock stuck) at all. Full suite re-run twice after this fix: **82/82 node:test passing both times**, ~4.4 minutes wall time (files now run sequentially rather than overlapping, an accepted tradeoff — correctness over speed for this pass), no hangs.
- **Verified (this whole batch — C4, C3, plus the test-infra fix)**: `npx tsc --noEmit` clean; `npm run test:unit` 82/82 node:test + 36/36 Vitest (up from 78/34 at the last checkpoint); `npm run build` clean.
- **B4, B7, B8 landed and verified** (continuing straight into B-priority per the "continue autonomously" instruction):
  - **B4 (tab-hidden integrity logging)**: `postAttemptEvent()` (new, `sessionApi.ts`) wired into the existing visibility handler — `tab_hidden`/`tab_visible` (with `hiddenForMs`) now logged to the already-existing `assess.attempt_event` table via the already-existing `POST /assess/attempts/:id/events` route, previously never called from the frontend at all. Found and fixed a real test-infra bug while adding its Vitest coverage: `vi.restoreAllMocks()` in `TestTakingView.test.tsx`'s `afterEach` was silently wiping the `mockResolvedValue` set at `vi.mock()` factory time (not just call history) — any component code chaining `.catch()` straight onto such a mocked call would throw synchronously the moment that happened. Fixed by re-establishing the resolved values in `beforeEach`.
  - **B7 (`beforeunload` guard)**: added, gated by an `exitingLifecycleRef` so the native prompt never fires after a genuine submit or Exit & Pause. Two new Vitest cases (prompt fires while active; doesn't fire after auto-submit) — the second deliberately reuses the existing, already-reliable auto-submit-at-zero flow rather than clicking through the submit-confirmation modal, after the modal path proved timing-flaky under fake timers (same class of issue as C4's earlier Previous-navigation lesson — noted, not re-litigated).
  - **B8 (server-side API lockdown)**: the policy question flagged earlier (\"which endpoints need protecting\") resolved by checking what `TestTakingView` actually calls — only `/assess/attempts/*`; nothing else, not even `/me`. New `backend/src/middleware/attemptLockdown.ts`, folded into `requireAuth.ts` itself (one change point instead of touching catalog/content/core/learn/admin's five inconsistent per-route `requireAuth` patterns individually) — rejects any non-allowlisted call with `423 ATTEMPT_LOCKDOWN_ACTIVE` while the caller has an `in_progress` (not `paused`) attempt. **Verified live against a real running dev server**, not just unit-tested: `db/scripts/manual/verify-b8-attempt-lockdown.ts` signed in as the real demo account and confirmed the full sequence (dashboard reachable → locked during attempt → attempt's own endpoints still reachable → dashboard reachable again after submit) — full pass. Dev server force-stopped afterward (Windows' `npm run dev:api` needed an explicit `taskkill` on the underlying PID — `TaskStop` alone left the port bound) and port 4000 confirmed clear via `netstat` before moving on.
  - **Verified (this whole B4/B7/B8 batch)**: `npx tsc --noEmit` clean; `npm run test:unit` 82/82 node:test (unchanged count — no new backend integration test needed beyond the live manual HTTP verification) + 39/39 Vitest (up from 36); `npm run build` clean; live HTTP verification passed against a real server.
- **Remaining in B**: B1/B2/B10 (browser back/forward and direct-URL navigation guard — needs `App.tsx`'s screen-routing architecture, not yet examined for this), B5 (second-tab warning), B6 (fullscreen request/exit handling). **Remaining overall**: C1 (paused vs. abandoned state distinction), C9 (needs product clarification on whether scheduled-test-windows exist at all — flagged, not guessed), D (blueprint-as-data — blocked on a real NTA bulletin source), E (image test type — needs the group concept built for the first time), F (test-type config consolidation), R1/R2/R3 reports.
- Stopping this session at this clean sub-boundary (A-priority, C3, C4, B4, B7, B8 all done and verified) before starting the larger remaining pieces of B (navigation guard, fullscreen) or moving to C/D/E/F.

### Session 2 (2026-08-31, continued)
- Resumed per this file's own "continue" instructions: read the Session log, re-verified nothing was silently left "in progress," picked up exactly where Session 1 stopped — the rest of B (B1/B2/B10, B5, B6).
- **Examined `App.tsx`'s actual routing architecture before assuming the B1/B2 lead's stated root cause was still accurate** (react-router-dom v7 is present, `useLocation`/`useNavigate` are wired, contrary to the audit's original "no router at all" framing): confirmed live via grep that (a) every `setCurrentScreen` call lives inside `App.tsx` itself, nothing else calls it, so no other mounted component can programmatically escape `test_taking`; (b) `currentScreen === "test_taking" && activeSession` is a hard render-tree override — no `Header`/nav/other screen ever mounts alongside `TestTakingView`; (c) none of the system_check/lobby/test_taking transition handlers call `navigate()`, so **no history entry is ever pushed for entering the exam** — the real bug is that the browser's history stack at that point is identical to whatever it was before the test even started, so the first Back press pops past the app rather than being interceptable as a same-document route change.
- **B1 fixed and shipped**: `TestTakingView.tsx` now pushes a throwaway `history` entry on mount and listens for `popstate`; on fire it re-pushes (neutralizing the navigation) and routes through the exact same `handleExitAndPause` confirm-and-pause flow the existing "Exit & Pause" button uses, guarded by the already-existing `exitingLifecycleRef` so an in-flight submit/exit doesn't get double-prompted.
- **B2 re-audited rather than separately fixed**: given the render-tree-override finding above, there's no in-SPA click path that can "render a different screen" during `test_taking` — the only real vector is B1's `popstate` case (now closed) or a full page reload, which was already caught by the existing E1/E4 non-dismissible resume-modal (`closeOnBackdropClick={false}`/`closeOnEscape={false}`) plus B8's server-side `423` lockdown from earlier this pass. Documented as closed-by-composition in `BUGS.md#B2` rather than writing redundant code.
- **B10 closed by B1's fix** (same `popstate` mechanism covers a deep-link/notification tap that manipulates history; a full-reload deep link falls to the same B2 coverage).
- **B5 fixed and shipped**: `BroadcastChannel` scoped to `lumen_attempt_${attemptId}`, announced on mount; any other tab replying flips a non-blocking amber banner in both tabs. Deliberately scoped exactly as the audit proposed (no optimistic-concurrency/schema change) — the underlying per-question last-write-wins autosave behavior is unchanged, just no longer silent.
- **B6 fixed and shipped, with a real cross-browser regression caught before shipping, not after**: fullscreen has to be requested from `LobbyView.tsx`'s "I Understand, Start Test" click — the only genuine synchronous user gesture in the whole flow (the 30s countdown's own auto-entry fires from a `setInterval`, and `TestTakingView` mounts off a lazy-loaded chunk — neither carries transient activation). `TestTakingView.tsx` tracks `fullscreenchange` and shows a non-dismissible, non-punitive "Return to Fullscreen" overlay (no auto-submit) whenever fullscreen isn't active, with its own button as a fallback real click. **Caught while implementing, before it shipped**: the Fullscreen API doesn't exist at all on iOS Safari/Chrome — an unconditional overlay would have permanently locked the exam behind a "return to fullscreen" prompt that could never clear on any iPhone, strictly worse than B6's original gap. Feature-detected (`document.fullscreenEnabled` + `typeof requestFullscreen === "function"`) and the whole mechanism no-ops where unsupported, verified by a dedicated test.
- **New Vitest coverage**, all in `TestTakingView.test.tsx` (5 new cases: popstate→confirm→pause→onCancel; no double-prompt once already exiting; BroadcastChannel banner; fullscreen overlay blocks/clears when the API is mocked as supported; overlay never appears when the API is mocked as absent) — required extending the existing `renderView` test helper to accept `onCancel`/`onCompleteTest` overrides (previously hardcoded to a fresh `vi.fn()` with no way to assert against it) and adding `pauseAttempt` to the file's mock-reset boilerplate in `beforeEach` (same "restoreAllMocks clears factory-time resolved values" lesson from Session 1's B4 work — applied proactively here, not rediscovered).
- **Verified (this whole B1/B2/B5/B6/B10 batch)**: `npx tsc --noEmit` clean; `npm run test:unit` 82/82 node:test (unchanged, no backend touched this batch) + 44/44 Vitest (up from 39 — 5 new TestTakingView cases), full suite run clean; `npm run build` clean (frontend + backend bundle, only pre-existing chunk-size warnings, no new ones).
- **B is now fully done**: B1-B10 all fixed/closed-by-composition/documented per `BUGS.md`, nothing left in this bucket.
- **C1 fixed and shipped, continuing straight into C per the "continue autonomously" instruction**: `abandoned` had been schema-legal since migration 018 but no live code path ever wrote it — every expired attempt, including one the student never opened past question 1, was force-scored as if it were a genuine "0/N" submission. Policy decision made explicitly (not left open, and not guessing an arbitrary N-hours grace-window number with no product source): an expired attempt with **zero** `assess.attempt_response` rows now becomes `abandoned`; anything with **≥1** response, however partial, still goes through the unchanged `scored` path. New `abandonAttempt` (`db/assess/test/attempt/attempt-flow.ts`) does the transition, re-checking the response count inside its own row lock (a response landing in the gap between a caller's check and the lock falls through to real scoring, never silently lost) and idempotent against an already-terminal attempt. `expiry.ts`'s `enforceExpiry`/`sweepExpiredAttempts` both route through a new shared `closeExpiredAttempt` helper so the lazy and eager (sweeper) paths can't drift apart on this decision. No migration needed — confirmed by grep that the frontend (`MyResultsView.tsx`) already had `abandoned` label/badge/gating logic built and waiting, just never fed a real value.
- **Found and fixed while landing this, not left broken**: the existing C3 test (`expiry-sweeper.test.ts`) and an older manual proof script (`db/scripts/manual/prove-te-p4-expiry-verify.ts`) both exercised exactly this bug's target scenario (an attempt started and never answered) and both hardcoded `scored` as the expected outcome — now factually wrong under the corrected behavior. Updated both rather than leaving them silently asserting stale behavior; added a new sub-test to `expiry-sweeper.test.ts` proving the ≥1-response case still scores normally (the other half of the policy). **Verified live**: full `expiry-sweeper.test.ts` (5 sub-tests) run directly against the dev DB, all passing.
- **Verified (this whole C1 batch)**: `npx tsc --noEmit` clean; `npm run test:unit` 83/83 node:test (up from 82) + 44/44 Vitest; `npm run build` clean.
- **Remaining overall (at that point)**: C9 (needs product clarification), D (blocked on a real NTA bulletin source), E (image test type), F (test-type config consolidation), R1/R2/R3 reports.
- Budget allowed continuing further this session: picked F1 and F4 next — both concrete, scoped, no product decision needed (unlike C9/D), and don't require E's "group concept" (a genuinely large, not-yet-built primitive that only E6/a future first-class image test type need — E1-E3's own bugs don't).
- **F1 fixed and shipped**: `assemble.ts`'s `LINE_CANDIDATE_SQL`/`LINE_AVAILABLE_SQL` descendant-scope match was `target.node_path || '%'` with no path separator before the wildcard — since `node_path` has no delimiter of its own between levels beyond the leading `/`, this also matched a *sibling* node whose `node_code` shares target's as a prefix (e.g. target `/PHY/U1` also matching sibling `/PHY/U10`), a real SQL correctness defect, not theoretical. Fixed to `|| '/%'` in both queries; verified the existing `qnm.node_id = $<n>::uuid` OR-branch in both already covers the target node itself, so dropping the exact-match case from the LIKE clause loses nothing, exactly as `BUGS.md`'s own proposed fix anticipated. **A mistake caught and fixed before it shipped**: my first attempt at this edit put an explanatory comment using `//` syntax *inside* the SQL template literal itself — since Postgres doesn't recognize `//` as a comment marker (only `--`), that would have sent invalid SQL to the database; caught by re-reading the diff immediately after applying it, moved the comment outside the template literal as a normal JS comment, and confirmed via `tsc`/live test run afterward that the actual SQL body is clean. New regression test `db/assess/test/generation/scope-prefix-match.test.ts` — rather than fabricating temporary `catalog.syllabus_node`/`content.question` fixture rows across several NOT-NULL-heavy tables in the shared dev DB just to prove a two-character wildcard change, it runs the exact same pattern construction against literal path strings through a real Postgres connection, including a sanity check proving the *old* pattern really did mismatch (so the test is proven to exercise a real defect). Verified live, passing.
- **F4 fixed and shipped**: adding a test type used to require independently-maintained edits in three files (`test-code.ts`'s `TestTypeCode` union, `sessionController.ts`'s `toLines()` if-chain plus a hand-written MOCK-exclusion regex in `hasCompletedPracticeTest`, and `assess.routes.ts`'s `z.enum([...])` allowlist) with nothing forcing them to agree. Added `TEST_TYPE_CONFIG: Record<TestTypeCode, {isSingleScope, countsAsPractice}>` to `test-code.ts`, scoped to only the two properties actually duplicated elsewhere (traced every consumer first — a default-duration or marking-scheme-key field would have been speculative, nothing reads either independently today); `assess.routes.ts`'s enum and `sessionController.ts`'s exclusion regex now both derive from it. `TypeScript`'s own `Record<TestTypeCode, ...>` typing means a new test type without a config entry fails to compile — real, structural protection against exactly this bug's failure mode, not just a comment asking future authors to remember. Left `deriveSessionModeFromTestCode`'s regex-based reverse mapping untouched per `BUGS.md`'s own caveat that direction can't be replaced by a lookup table the same way. New `db/assess/test/definition/test-code.test.ts` (pure logic, no DB needed) proves the two derived lists match today's values and that the new regex construction reproduces the old hand-written pattern's exact matching behavior.
- **Verified (F1+F4 batch)**: `npx tsc --noEmit` clean; `npm run test:unit` 88/88 node:test (up from 83 pre-F1 — 1 new F1 case + 4 new F4 cases = 5) + 44/44 Vitest; `npm run build` clean.
- **F is now fully done** (F1-F4 all fixed/confirmed-non-reproduced per `BUGS.md`).
- **E2 fixed and shipped, continuing straight into E per the same autonomous-operation rule**: `content.question.has_image` was a plain boolean the real import pipeline never touched at all (confirmed by grep — every imported question defaulted to `false` regardless of whether it actually had an image). `db/migrations/028_has_image_computed.sql` adds `content.trg_asset_sync_has_image()`, an `AFTER INSERT/DELETE/UPDATE OF question_id, target_role` trigger on `content.asset` that recomputes the affected question's `has_image` from scratch on every change (approach (b) from `BUGS.md`'s own proposed fix — kept the denormalized column since it has exactly one live reader, maintained it via trigger instead of the far larger blast radius of dropping it), plus a one-time backfill. The predicate deliberately mirrors `envelope.ts`'s own real "does this render during the exam" definition exactly (`target_role in ('stem','option')`, no `asset_type` filter) rather than inventing a stricter one that could itself drift from actual behavior. **Checked before applying** (same discipline as A1): a live drift query first — zero rows differed, so this was a pure additive change, not a data-correcting one. **Verified live**: ran the migration + verify script against the dev DB; proved the trigger's actual insert/insert-unrelated/delete behavior in a rolled-back transaction against live data; captured those same three assertions as a new permanent test, `db/content/has-image-trigger.test.ts` — `db/content/` had zero test coverage before this, so also added `"db/content/**/*.test.ts"` to `package.json`'s `test:unit` glob so this is actually part of the tracked suite going forward, not a one-off check.
- **Verified (E2 batch)**: `npx tsc --noEmit` clean; `npm run test:unit` 89/89 node:test (up from 88) + 44/44 Vitest; `npm run build` clean.
- **Remaining overall**: C9 (needs product clarification on whether scheduled-test-windows exist at all — flagged, not guessed), D (blueprint-as-data — blocked on a real NTA bulletin source, cannot proceed without the user supplying an actual citation), E3/E6 (E3 — broken/unreachable asset URLs still drawable — needs a new `is_reachable`/`last_verified_reachable_at` column plus a scheduled reachability-sweep job, a real design choice flagged in `BUGS.md` rather than assumed; E6 needs the group concept, forward-looking only, N/A today), R1/R2/R3 reports, Definition-of-done checklist.
- Stopping at this clean sub-boundary (A, B, C1/C3/C4, F, E2 all done and verified) — next natural chunk is E3, which needs a scheduled-job design decision flagged in `BUGS.md` (synchronous per-request reachability check would be too slow; the proposed design is a periodic sweep writing a reachability column, same infra shape as C3's sweeper) before implementing.

### Session 3 (2026-08-31, live-bug interrupt)
User reported two live bugs against the running app ("can't resume the paused test", "still seeing repeated questions") and asked for a first-class image-based test feature. Departed from the planned E3 continuation to investigate and fix the live reports first, per the user's explicit priority — logged here rather than in a separate tracker since it's the same subsystem this whole directive covers.

- **"Can't resume the paused test" — root-caused to two distinct real bugs, not one, both found live before guessing at a fix:**
  1. **B6 regression**: this session's own earlier fullscreen work (B6) only ever requested fullscreen from `LobbyView.tsx`'s real click — but `App.tsx`'s `handleResumeSession` (the "Resume Test" button on the reload-survival modal) jumps straight into `TestTakingView`, bypassing Lobby entirely, so a resumed attempt never got a fullscreen request at all, yet the (then-)blocking "must be fullscreen" overlay still applied. Confirmed live via a Playwright run of the actual pause→reload→resume flow (not just reasoning about the code) that the overlay really did cover the exam after resume.
  2. Fixing the request call alone wasn't sufficient — a second live Playwright run showed the overlay could still appear because `requestFullscreen()` resolves asynchronously and `TestTakingView` can render its first frame (deciding whether to show the overlay) before the browser's `fullscreenchange` event fires. Rather than keep chasing browser-specific timing, **redesigned B6 from a hard-blocking overlay into a non-blocking banner** (same pattern as B5's tab-warning banner) — the exam is never covered or disabled by the fullscreen nudge again, regardless of timing or whether the request ever succeeds. Updated `TestTakingView.test.tsx`'s B6 cases and added a new Playwright regression test (`tests/happy-path.spec.ts`) driving the real pause→reload→resume→answer→submit path end to end.
  3. **A second, independent bug found while verifying the Playwright fix**: `POST /api/auth/demo/reset` wasn't on B8's lockdown allowlist — a demo account left with a genuinely stuck `in_progress` attempt (exactly what a failed test run, or a real crashed tab, produces) could never recover, because the one endpoint whose entire purpose is wiping the account back to clean 423'd on exactly the state it exists to clear. Added `/api/auth/demo` to `attemptLockdown.ts`'s allowlist.
  4. **A third, independent, more fundamental bug found while investigating**: `enforceExpiry`/`sweepExpiredAttempts` applied deadline expiry to `paused` attempts, not just `in_progress` ones — but `paused_ms_total` is only credited retroactively inside `resumeAttempt`'s own transaction once a pause actually *ends*, never while it's still open. A student paused for longer than whatever time was left on the clock looked "expired" to that stale formula even though pausing is this app's own sanctioned stop-the-clock mechanism. Confirmed live against real data: a genuine paused attempt sat ~9 minutes from being force-closed by exactly this bug. Fixed both functions to only ever apply expiry to `in_progress` attempts; added two new sub-tests to `expiry-sweeper.test.ts` proving a paused attempt survives arbitrarily far past its nominal deadline via both the lazy (`enforceExpiry`) and eager (sweeper) paths.
- **"Still seeing repeated questions" — investigated against real exposure data before touching code**, per this directive's own standing discipline. Queried live `assess.user_question_seen`: some accounts showed avg `times_seen` of 8-12x, worst case 42x. Root cause: `catalog.syllabus_node` units are seeded with exactly 30 published questions each (subjects have 330-365) — a handful of unit-scoped attempts genuinely exhausts the unseen pool, and `LINE_CANDIDATE_SQL`'s existing D-2 policy (recycle least-recently-seen once unseen runs out) is already doing exactly what it was designed to do. **This is a content-volume limit, not a fixable query bug** — reported honestly rather than claiming a fix that doesn't exist. One real, separate latent risk was hardened regardless: `min(last_seen_at)`'s fallback ordering had no explicit tiebreaker, and `startAttempt`'s own serve-time exposure upsert (A6) stamps a whole attempt's served questions with the identical timestamp, so a fully-saturated pool's tie-order rested on Postgres's unspecified (not contractually random) behavior rather than this query's own seed. Added the same seed-keyed md5 hash already used for the unseen bucket as an explicit tiebreaker for the seen bucket too. **Honest about what a live reproduction did and didn't show**: a dedicated test (`repeat-rotation.test.ts`) that fully saturates a real small pool in one batch and draws twice more with different seeds passed both *before and after* the fix on this Postgres build/plan — the hypothesized "deterministic physical-order stickiness" didn't actually reproduce live. Kept the fix and the test anyway as a real hardening (explicit seed-driven order beats relying on unspecified tie-breaking that a future PG version/plan/stats change could legally alter) but did not oversell it as the proven root cause of the user's report — that's the pool-size limit above, which no code change here can fix (would need more real, authored questions per unit).
- **Image-based test type built and shipped end to end (E7, new entry in `BUGS.md`)** — the user's third ask this session ("build a test for image based test in the whole application system"). New `assess.test_blueprint.has_image_only` column (`029_blueprint_has_image_only.sql`), threaded through `create-test.ts`/`create-practice-test.ts`/`assemble.ts` as an optional per-line filter, the same shape as the existing `difficultyBand`/`questionFormat` filters — deliberately not a new `TestTypeCode`, per F4's own "don't add a taxonomy dimension where an existing one fits" lesson. New `mode: "image-practice"` on `POST /assess/sessions` resolves real per-subject `has_image=true` counts server-side and builds one blueprint line per non-empty subject with exactly that many questions (never padding past real availability). `catalogTreeController.ts` now also returns `imageQuestionCount` per subject/unit; new "Image-Based Practice" card in `TestListView.tsx`; `deriveSessionModeFromTestCode` recognizes `MOCK`+`IMAGES` so a resumed attempt renders correctly. Consolidated the four independently-repeated `SessionMode` union literals into one shared type while touching them anyway (same F4-style discipline).
- **Verified live through the real pipeline, not just typechecked**: `db/assess/test/generation/image-only-blueprint.test.ts` proves the filter end to end (real `createPracticeTest -> startAttempt -> getAttemptEnvelope`) and that `PoolInsufficientError.available` honestly reflects the has_image-filtered pool, not the subject's larger total. A full Playwright journey drives the actual UI button through system-check/lobby/exam/submit and asserts a real loaded `<img>` is present (not best-effort — this mode guarantees one on every question). **A real test-helper bug found and fixed along the way**: the shared `answerFirstQuestionAndSubmit` helper's `main button first()` selector resolved to the stem image's own "Zoom image" button (which renders before the answer options whenever a stem image exists) instead of an answer option, opening the fullscreen zoom lightbox and blocking the later Submit click — every other journey in the suite happened to avoid this because images were rare/absent on the questions they served; this new mode guarantees one on every question, which is exactly what surfaced it. Confirmed this is a test-selector fragility, not a real production concern (a real user visually distinguishes the small zoom icon from the answer options) — fixed the shared helper (excludes `[aria-label="Zoom image"]`), benefiting every journey in the file.
- **Content-volume caveat, stated honestly**: the live bank has only 15 `has_image=true` published questions total across all four subjects (CHEM 9, BOT/PHY/ZOO 2 each) — the feature is fully correct and scales automatically as more image content is authored, but today's test is necessarily small. UI shows the real count before launch and disables the button with an honest message if it's ever zero.
- **Verified (this whole Session 3 batch, all three asks)**: `npx tsc --noEmit` clean; `npm run test:unit` 97/97 node:test (up from 89 at session start) + 44/44 Vitest; `npm run build` clean; full `happy-path.spec.ts` Playwright suite run serially (`--workers=1`) — 10/11 passing, only the pre-existing unrelated stale-copy test failing (see below).
- **Noted, not fixed (unrelated to this session's work, pre-existing)**: running the full `happy-path.spec.ts` file with Playwright's default parallelism produces `409`-conflict failures — confirmed via a `--workers=1` re-run that these are a shared-demo-account race between concurrently-running journeys, not a real regression (all pass serially). Separately, "Full Mock entry point reflects the real syllabus-completion gate" fails even serially — the live button reads "Complete 1 practice test to unlock", not either string the test's selector expects (`Locked (Complete Syllabus Tracker)` / `Start Full Mock Test`) — stale test copy from a prior copy change, unrelated to anything touched this session. Flagging both rather than silently leaving them failing or fixing tests outside the reported scope.
- Stopping this session at this clean boundary: all three user-reported items addressed (resume bug fixed with two independent root causes closed, repetition investigated with an honest content-volume finding plus a real hardening fix, image-based test type built and verified end to end). Remaining from the underlying test-layer-hardening directive: C9, D, E3/E6, R1-R3, done-when checklist — unchanged from before this interrupt, ready to resume next session.
