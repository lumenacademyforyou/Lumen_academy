# Defect Backlog

Things noticed mid-fix that are outside the current phase's scope. Add a row, do not fix it in the same pass.

| ID | Noticed during | Description | Triaged to |
|---|---|---|---|
| DB-06 | P6 (BUG-18 investigation) | **All 82 of 82** active `learn.unit_material` rows point to Google Drive files that are **not** shared "Anyone with the link — Viewer" — confirmed live via `db/scripts/check-drive-material-access.ts` (a plain HTTP client with no Google session, i.e. exactly what a real student's browser would hit), every single one redirects to `accounts.google.com`'s sign-in wall or returns 401/403 instead of the file. This is a **total** gap across every subject (bot/chem/phy/zoo), not a partial one. The app code itself (frontend `CoursesView.tsx`'s iframe preview + `downloadUnitMaterial`, backend `unitMaterialController.ts`'s redirect) is correct and was already verified/committed by a prior session (`a02417b`) — the actual root cause is that the 82 Drive files were never made public, exactly the risk `docs/NEET_TOOL_FIX_TRACKER.md` had already flagged as "needs a human, not a script" and left unverified. **This session's fix, in scope:** the frontend no longer silently saves a fake "material.pdf" when the download redirect actually lands on an HTML sign-in page (checks `content-type` before saving the blob), the download-failure message is now one clear sentence instead of a raw network/CORS error string, and the in-app viewer modal now shows a static hint ("seeing a Google sign-in page instead...") since cross-origin iframe content can't be inspected to detect this automatically. **Not fixed, and can't be fixed in code:** making the 82 files actually public requires someone with edit access to the source Google Drive account/folder to bulk-update sharing settings — re-run `db/scripts/check-drive-material-access.ts` after that to confirm. | **Needs the user (or whoever owns the Drive account)** — this is an out-of-code action, not a future coding session's task. Once sharing is fixed, BUG-18's done-when criterion ("view opens inline and download saves a valid, openable file... for every subject") can actually be verified live. |
| DB-05 | P5 (BUG-16 i18n layer) | The plan's own BUG-16 spec asks for "no hardcoded user-visible strings anywhere." This session converted the 443-key inline dictionary to real `en.json`/`ta.json` resource files, added persistence + `<html lang>`, and split ui_lang from question_lang (BUG-17) — but did **not** attempt a full sweep of every component for strings never wrapped in `t()` at all. Confirmed live, spot-checking just `Header.tsx`: many strings ("My Profile", "Course & My Plan", "Close", "Save Changes", "Current Package", "Aspirant Name", "Danger Zone" — some wrapped in `t()` with no matching dictionary key, some not wrapped at all) render in English regardless of `ui_lang`. This pattern likely repeats across the other 20+ files that call `useLanguage()`. Not fixed: a full audit is hundreds of strings across dozens of files, and the plan's own acceptance criterion ("click through every route in Tamil — no untranslated strings") requires actually exercising every screen, which this session didn't do end-to-end. | Not yet triaged — needs a dedicated pass per major screen (Header/Dashboard/Analytics/Profile/etc.), each wrapping remaining strings in `t()` and adding the corresponding `en.json`/`ta.json` entries, then a real click-through in Tamil per the plan's exit gate. |
| DB-04 | P3 (BUG-10 verification) | The plan's own Layout Checklist (13.3) asks for 3 question types (long ~800-word text, tall portrait image, wide landscape image) x 3 widths = 9 combinations. This session verified the fixed-viewport/internal-scroll layout live at all 3 widths, but the demo bank's Physics-practice set happened to serve short text-only MCQs at every width tested — the tall-image and 800-word-text cases were never actually exercised against the mobile off-canvas-palette layout this session also just changed. The layout code itself (`overflow-y: auto` + `min-h-0` on the body region, `QuestionImage`'s reserved-space + `object-fit`-equivalent framing) has no reason to behave differently for longer content, but that's an expectation, not a live-verified fact. | Not yet triaged — needs a session that either finds a real long-text/tall-image question in the live bank or seeds one, then re-runs `tests/_bug10-layout-check.spec.ts`-style verification against it specifically. |
| DB-03 | P8 (BUG-23 fix) | Same root cause as BUG-23 (App.tsx's `attempts` state is ephemeral, client-only, and never reflects real cross-session history), but a different visible symptom, not explicitly named in the original 31-item list: `TestListView.tsx`'s "recent attempts" strip (`recentAttempts`, line 175) and `App.tsx`'s daily-reminder "did they test today" logic (lines 208/260) both still read from this same unreliable array — after a fresh reload/new device/new login, both would show empty/stale even for an account with real history, exactly like the Dashboard's "Tests Taken" tile did before this session's fix. Not fixed here: BUG-23 named the Dashboard count specifically, and this is architecturally a bigger change (App.tsx's `attempts` state would need to become server-backed everywhere it's read, not just wherever it's currently misread) — a supersets fix, not a quick add. | Not yet triaged — recommend folding into whichever session eventually replaces `App.tsx`'s `attempts`/`activeAttemptId` state with real server data end-to-end. |
| DB-02 | P2 (BUG-08 investigation) | No server-side validation exists at test-publish time to reject a test whose blueprint the live question pool can't satisfy (the plan's own BUG-08 fix spec asks for this). Not fixed: `PoolInsufficientError` already fires live at attempt-creation time with a clear, structured, user-facing message (confirmed handled in `TestListView.tsx`/`CoursesView.tsx`/`DashboardView.tsx`), so a student never hits a silent failure today; the gap is purely "catch it earlier, at admin publish time" for FIXED-mode papers via `ingestFixedPaper` — which recon (`CONTEXT.md`) already found has zero live HTTP callers, so there's no admin flow yet to attach this validation to. | Not yet triaged — pair with whenever `ingestFixedPaper` gets a real admin route. |
| DB-01 | P2 (BUG-03 reconciliation fix) | `startAttempt` (`db/assess/test/attempt/attempt-flow.ts`) calls `assembleForAttempt(testId, userId)` from inside its own open transaction (a `client` checked out via `pool.connect()`), but `assembleForAttempt` and everything it calls internally use the shared `pool.query(...)` directly, not that transaction's client — requesting extra connections from the same pool (`db/shared/pool.ts` caps it at 4) while one is already held open. Confirmed live: the *identical* bug in this same file's `loadSectionSchemes` (also called mid-transaction, also via bare `pool.query`) deadlocked 4 real Postgres backends the instant 4 `submitAttempt` transactions ran concurrently — fixed in this session (now takes the transaction's client as a parameter). `assembleForAttempt`'s equivalent risk is still present and unfixed: **4 or more students starting a BLUEPRINT-mode test at the exact same moment would deadlock the same way in production**, every one of them stuck forever. Not fixed here because it wasn't exercised by anything in this pass (`startAttempt` was never run with real concurrency by this work — my only new concurrent path was `submitAttempt`'s), and properly threading a client through the whole assembly pipeline (candidate-pool selection, exposure-ledger queries, etc.) is a larger, separate change. | Not yet triaged — needs its own session; recommend before any real concurrent-load testing. |
